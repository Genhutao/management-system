# 页面视觉风格总结 — 前端设计参考文档

> 参考样本：腾讯云轻量云控制台（服务器应用管理页）
> 适用场景：B 端管理后台 / SaaS 控制台 / 数据管理系统的前端视觉规范设计
> 文档定位：供前端设计师与开发人员复用的风格基线（Design Tokens + 组件样式 + 实现建议）

---

## 一、设计风格定位

**企业级 B 端 / SaaS 控制台风格**，属于 **Ant Design / TDesign 设计体系** 的典型实践。

| 关键词 | 说明 |
|:---|:---|
| 功能导向 | 一切视觉为操作效率服务，装饰性元素极少 |
| 信息密度高 | 页面承载大量业务数据，排版紧凑但不拥挤 |
| 效率优先 | 高频操作入口显眼，快捷键与批量操作友好 |
| 克制理性 | 无大面积渐变、无花哨动效，色彩收敛 |

一句话概括：**深色导航 + 浅色内容的"双模式"框架，蓝色品牌主色，小圆角 + 细边框的克制卡片体系。**

---

## 二、整体布局框架

```
┌─────────────────────────────────────────────────────────────┐
│  顶部全局导航栏（深蓝）   Logo │ 搜索 │ 账户 │ 消息通知      │
├──────────────┬──────────────────────────────────────────────┤
│  左侧边栏     │  面包屑导航                                    │
│  （深灰蓝）   │  ┌──────────────────────────────────────┐    │
│              │  │ 标签页 Tabs（下划线激活）              │    │
│  多级折叠菜单 │  │ 信息提示条（蓝色左侧边框）              │    │
│  当前项高亮   │  │                                      │    │
│              │  │  内容卡片区（白底 + 细边框/极淡阴影）    │    │
│              │  └──────────────────────────────────────┘    │
│              │  右侧浮动工具栏：头像 / 帮助 / 消息            │
└──────────────┴──────────────────────────────────────────────┘
```

| 区域 | 特征 | 实现建议 |
|:---|:---|:---|
| 顶部导航 | 固定高度（约 48-56px），深蓝品牌色，全局可见 | `position: sticky` 或 `fixed` 布局 |
| 左侧边栏 | 固定宽度 200-220px，深色背景，可折叠为 64px 图标模式 | 采用 `Layout` 侧边栏组件 |
| 主内容区 | 浅灰背景，最大宽度限制或全宽自适应 | 大屏注意留白，最小适配宽度 1280px |
| 面包屑 | 顶部次级导航，反映当前层级位置 | `首页 / 产品 / 服务器` 格式 |
| 右侧悬浮工具栏 | 头像、帮助、消息快捷入口 | 悬浮定位，不遮挡主内容 |

---

## 三、色彩系统（核心 Token）

### 3.1 色板总览

| 用途 | 色值 | 说明 |
|:---|:---|:---|
| 顶部导航 | `#0064C8` 渐变（深蓝） | 品牌主色延伸 |
| 左侧边栏 | `#1a1d29` | 深灰蓝（偏黑），比阿里云更"黑" |
| 主背景 | `#f0f2f5` | 浅灰，衬托白色卡片 |
| 卡片背景 | `#ffffff` | 纯白 |
| 品牌主色（按钮/链接） | `#0064C8` | 冷蓝偏青，区别于阿里云橙、华为云红 |
| 成功 / 运行中 | `#52c41a` | 绿色状态点 |
| 文字主色 | `#333333` | 正文 |
| 次要文字 | `#666666` | 辅助说明 |
| 弱化文字 | `#999999` | 占位、禁用、说明 |
| 边框 / 分割线 | `#e8e8e8` | 极浅灰，细 1px |

### 3.2 设计 Token（CSS 变量）

```css
:root {
  /* 品牌色 */
  --brand-primary: #0064C8;
  --brand-primary-hover: #0050a0;
  --brand-primary-active: #004a99;
  --brand-primary-disabled: rgba(0, 100, 200, 0.4);

  /* 功能色 */
  --success: #52c41a;
  --warning: #faad14;
  --error: #ff4d4f;
  --info: #1890ff;

  /* 中性色 */
  --text-primary: #333333;
  --text-secondary: #666666;
  --text-tertiary: #999999;
  --text-disabled: #bfbfbf;
  --border-base: #e8e8e8;
  --border-input: #d9d9d9;
  --bg-base: #f0f2f5;
  --bg-component: #ffffff;

  /* 深色导航色 */
  --sider-bg: #1a1d29;
  --sider-text: #8b949e;
  --sider-text-hover: #ffffff;
  --sider-active-bg: #0064C8;

  /* 圆角 */
  --radius-xs: 2px;   /* 标签、状态点 */
  --radius-sm: 4px;   /* 按钮、输入框 */
  --radius-md: 8px;   /* 卡片、弹窗 */

  /* 阴影（极克制） */
  --shadow-card: 0 2px 8px rgba(0, 0, 0, 0.04);
  --shadow-pop: 0 4px 12px rgba(0, 0, 0, 0.12);

  /* 间距（8px 基数网格） */
  --space-xs: 8px;
  --space-sm: 16px;
  --space-md: 24px;
  --space-lg: 32px;
}
```

---

## 四、字体与排版

| 层级 | 字号 | 字重 | 用途 |
|:---|:---|:---|:---|
| 页面大标题 | 24px | 600 | 页面级标题 |
| 区块标题 | 16-18px | 600 | 卡片 / 模块标题 |
| 正文 | 14px | 400/500 | 默认正文、表单项 |
| 辅助说明 | 12px | 400 | 提示、注释、时间等 |

**字体栈：**

```css
font-family: -apple-system, BlinkMacSystemFont, "Segoe UI",
             "PingFang SC", "Microsoft YaHei", "Helvetica Neue", Arial, sans-serif;
```

**排版规范：**
- 行高 1.5-1.6，中文正文 14px 时建议 `line-height: 22px`
- 数字与表格数据建议等宽数字字体（`font-variant-numeric: tabular-nums`）
- 标题层级最多三级，避免过深嵌套

---

## 五、组件风格要点

### 5.1 按钮体系

```
主按钮：实心品牌蓝
  bg: #0064C8   text: #fff   hover: #0050a0   圆角 2-4px
次按钮：白色底 + 边框
  border: 1px solid #d9d9d9   text: #333
  hover: border #0064C8 / text #0064C8
危险操作：文字色表达
  color: #ff4d4f   hover: bg #fff1f0
链接按钮：无边框无下划线
  color: #0064C8
```

- 按钮高度规范：大 40px / 默认 32px / 小 24px
- 同一操作区只允许一个主按钮，避免层级混乱

### 5.2 表单与输入

- 输入框高度 32px，边框 `#d9d9d9`，圆角 4px
- Focus 状态：品牌蓝边框 + 轻微外发光（`box-shadow: 0 0 0 2px rgba(0,100,200,.1)`）
- 单选 / 复选：自定义样式，选中态品牌蓝填充
- 错误状态：红色边框 + 下方错误提示文字

### 5.3 标签页（Tabs）

- 主标签：**下划线风格**，激活态品牌蓝 + 加粗，未激活灰色
- 二级标签：胶囊式（圆角矩形底）
- 标签切换无过度动效，即时响应

### 5.4 卡片容器

```css
.card {
  background: #fff;
  border: 1px solid #e8e8e8;          /* 以边框分割为主 */
  border-radius: 8px;
  box-shadow: 0 2px 8px rgba(0,0,0,0.04);  /* 极淡阴影为辅 */
  padding: 24px;
}
```

- 卡片内标题 + 操作按钮放在右上角（标题左、操作右）
- 卡片之间间距 16px，不叠加阴影

### 5.5 信息提示条

- 特征：**左侧色块边框 + 浅色文字**，而非整块填充背景
- 示例：蓝色 `border-left: 3px solid #1890ff` + `bg: #e6f4ff`（或透明）

```
┌────┬──────────────────────────────┐
│ 蓝 │ 轻量提示文案，解释当前状态    │
└────┴──────────────────────────────┘
```

### 5.6 状态标识

- 用**彩色圆点 + 文字**表达状态：绿=正常/运行中、红=异常、灰=停用
- 圆点直径 6-8px，与文字间距 4px

### 5.7 图标系统

- 线性图标（Line style），1.5px 描边，统一尺寸 16px / 24px
- 左侧导航可带彩色状态指示点
- 图标库推荐：自定义 SVG / @ant-design/icons / TDesign Icons

---

## 六、间距与圆角规范

| 维度 | 规范 | 说明 |
|:---|:---|:---|
| 间距网格 | 8px 基数（8/16/24/32） | 所有间距取 8 的倍数 |
| 内容区边距 | 24px | 卡片与页面边缘 |
| 卡片内边距 | 24px | 卡片内容与边缘 |
| 组件间垂直间距 | 16px | 卡片之间、段落之间 |
| 表单项垂直间距 | 24px | 表单行间距 |

| 组件 | 圆角 |
|:---|:---|
| 标签、状态点、胶囊 | 2px |
| 按钮、输入框 | 4px |
| 卡片、弹窗、下拉面板 | 8px |

**设计原则：操作控件小圆角，容器卡片稍大圆角，整体保持克制。**

---

## 七、实现建议

### 7.1 推荐技术栈

| 方向 | 推荐 |
|:---|:---|
| 组件库 | Ant Design Vue/React、TDesign（腾讯开源）、Element Plus |
| 图标 | @ant-design/icons / TDesign Icons / 自定义 SVG |
| 样式方案 | CSS Modules、Tailwind/UnoCSS + Design Token、或 Styled Components |
| 布局 | 组件库 Layout（Sider + Header + Content） |

### 7.2 响应式适配

- 最小适配宽度 **1280px**（B 端控制台通常不做移动端深度适配）
- 侧边栏可折叠为 64px 图标模式
- 内容区大屏全宽自适应，注意控制最大宽度避免过度拉伸

### 7.3 无障碍与对比度

- 深色侧边栏文字与背景对比度建议满足 WCAG 4.5:1
- 禁用状态不只用颜色区分，配合图标或文字
- 交互元素 Focus 状态必须可见（键盘可达性）

---

## 八、差异化参考（与同类产品对比）

| 云厂商 | 主色 | 视觉特征 |
|:---|:---|:---|
| 腾讯云 | 蓝 `#0064C8` | 冷色调偏青、深色侧边栏更"黑"、提示条用左侧色块边框、整体更"轻"更现代 |
| 阿里云 | 橙 | 暖色调 |
| 华为云 | 红 | 红色系 |

**可借鉴亮点：**
1. 提示条用左侧边框而非填充色 —— 信息层级更轻，不抢内容视觉
2. 深色导航 + 浅色内容的双模式 —— 导航稳重、内容区清爽
3. 阴影极克制，靠边框分割 —— 页面更"平面"、更现代

---

## 九、总结

这套风格的**核心设计语言**：

> **蓝色品牌 + 深色导航 + 浅色内容 + 细边框克制卡片 + 小圆角 + 8px 网格 + 状态彩点**

适用于：学管会综合管理系统、教务管理、云服务控制台等一切**数据密集型 B 端后台**。落地时优先固化 Design Token（色板/圆角/间距/阴影），再基于组件库搭建，可保证多页面视觉一致性。
