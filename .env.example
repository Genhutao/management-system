# 复制为 .env 后填写；.env 不入 git（根目录 .gitignore 自行确认追加一行 ".env"）
# JWT 签名密钥：>=32 位随机串。生成：openssl rand -hex 32
JWT_SECRET=change-me-openssl-rand-hex-32
# AI/福利网关密钥的落库加密密钥：>=32 位随机串，与 JWT_SECRET 不同值。
# 一旦定下绝不更换——换了之后库里 enc:v1: 密文全部解不开，AI 配置与福利网关需要重新录密钥。
CRYPTO_SECRET=change-me-openssl-rand-hex-32-different
