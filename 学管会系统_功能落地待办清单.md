# 学管会综合管理系统 · 功能落地待办清单

> 基线：本清单对照《学管会综合管理系统_全业务功能完整实操指南》逐条核过代码。
> 结论：**系统实现了文档描述的界面与数据流转外形，但文档中承载制度刚性的若干承诺尚未实现**。
> 代码位置均为 `backend/` 下相对路径，便于原作者定位。
>
> **进度小结（2026-09-26）**：上述结论是**审计当时**的状态。此后 **A（P0 制度底座）、B（数据完整性）、C（登录与会话）、D（安全）四组已全部落地并逐项实测**（C 组按使用方要求暂缓 C3 口令策略、C4 会话生命周期），**E-1 已落地 8 行**（含 5.1 任免联动与 5.3 AI 排班真实调用，详见下文「E-1✅」「E-1✅ / E-2✅」「2026-09-26 晚」三轮落点段落），**E-2 的 3.1 时段枚举、5.2 灵活调分、5.2 标兵公示、6.2 福利兑换前置与透传终端五项已闭合**，**F-5 前端静态资源已改为同源本地文件**。仍需处理的是 **E-1 残留**（6.1 播音库有路由无界面、素养测评出题端无 API、APK 产物未构建验证）、**E-2 6.1 素材工坊无本地管理**、**「晚」轮实测发现的既有洞已按使用方口径闭合：全校违纪台账整表导出（`export-csv`）现仅限档案导出岗/部长/技术维护组/持打表权副部长，`GET /deductions` 分页列表仍对全体登录用户可读（刻意保留的违纪公示口径）**，**「深夜」轮再补三项：宿管终端历史留痕卡片可点击查看详细信息（新增只读端点 `GET /dorm/inspections/:id`，楼栋过滤逐字重放列表口径、不存在与越权合并 404）、前端断图 `onerror`→`src` 自循环导致的内存溢出已修（改一次性监听 + 零网络内联占位，实测 error 事件 8081→3）、《API接口文档.md》按运行中服务真实路由表整体重核并纠正 6 条过期结论**，以及 **F 组上线前置的其余四项**（合规文案、岗位与建档落地、改动提交、README）。各组状态以表内「状态」列为准。

---

## 0. 两个前提决策（2026-09-25 已由使用方确认）

| # | 问题 | 结论 | 已产生的影响 |
|---|---|---|---|
| D1 | **部署在校园内网还是公网？** | **暂定公网部署** | 按公网口径完成 C/D 组：会话改 HttpOnly Cookie + `Secure`、CORS 白名单化、福利网关 SSRF 防护、JWT 密钥与 DB 路径外置、上传体积上限、危险操作 step-up。**公网假设下仍未闭合**：无 HTTPS 终止方案、无登录限速/锁定、口令策略（C3）与会话生命周期（C4）被推迟 |
| D2 | **学校是否有企业微信 / 钉钉 / 统一身份认证？** | **没有** | 教师侧（宿管、导出人员）继续用自建账号，因此 C2 的宿管三要素登录加固与 C9 手机号变更联动是必要项而非可选项；未预留扫码登录通道 |

---

## A. 制度与安全底座 —— **已完成并实测通过**

| 项 | 问题 | 位置 | 状态 |
|---|---|---|---|
| Casbin fail-open | 引擎未初始化时全量放行；日志谎称有 role 兜底 | `middleware/casbin.go`、`cmd/server/main.go` | ✅ 启动失败即 fatal；`Enforcer==nil` 返回 503 |
| tech_admin 硬编码豁免 | 绕过整个策略引擎 | `middleware/casbin.go` | ✅ 移除，改由 `/api/v1/*` 策略覆盖 |
| 旧宽策略残留 | 策略持久化在 `casbin_rule` 且只增不删，收紧不生效 | `middleware/casbin.go` | ✅ 启动时显式撤销两条过宽记录（已实测） |
| 部员可任意打表/删他人记录 | 与文档"打表权严格集中在技术部副部长"直接冲突 | `controller/deduction_controller.go` | ✅ 新增 `requireDeductionAuthority`，实测越权返 403、合法副部长返 200 |
| AI 编造识别结论 | 未配置 Key 时返回硬编码"识别报告"并带 `deduct_points:5`，且恒返回 `nil` error | `pkg/ai/client.go` | ✅ 全部删除；新增 `real/disabled/failed/unknown` 状态，非 real 不产出结论 |
| 无照片伪造占位证据 | `sample_violation.jpg` / 前端 Unsplash 链接 | `controller/dorm_controller.go`、`static/app.js` | ✅ 无原图直接 400 拒绝 |
| 撤销即硬删除 | 违反"终生留痕"（A 组当时仅补了权限与错误检查） | `controller/deduction_controller.go` | ✅ 已由 B3 闭合：撤销改软删除（`status='revoked'` + 撤销人/时间/原因），台账仍可见、合计自动排除 |

**验证方式**：`go build ./...`、`go vet ./...` 通过；新增 `pkg/ai/client_p0_test.go` 8 个用例全绿；HTTP 层用真实服务跑过 13 条越权/正向断言。
**遗留**：测试文件是否保留待定；`xgh_server`（仓库内 54MB Linux 二进制）**不含以上任何修复，部署前必须重新构建**。

---

## B. 数据完整性（让"档案""留痕""防重复"真正成立）—— **✅ 已完成（2026-09-24，52 项端到端断言全绿）**

落点：`model/models.go`（`DeductionRecord` 新增 `StudentID`/`SourceInspectionID`/`SourceSubjectID` 与撤销四字段；新增 `InspectionSubject`、`OperationLog` 两表）、`controller/deduction_controller.go`（重写）、`controller/dorm_controller.go`、`controller/student_controller.go`、`service/roster.go`、`controller/audit.go`、`cmd/backfill-deduction-student/`。

验证证据（独立目录、独立数据库、端口 18090）：纸条上报名单拆 3 人并正确标出 1 人名册外；批量转入生成 2 条且每条绑定主键、重复提交返 409、勾选名册外学生返 400；在册他室的学生被拒并提示"疑似冒名"；撤销后原记录仍在台账、合计已排除、该名单可重新打表；清空名册需 `confirm=DELETE_ALL_ROSTER`，存在关联记录时先返 409，确认后只解除外键不删扣分；`operation_logs` 落 5 类动作；`casbin_rule` 无残留过宽策略。

回填历史数据：`go run ./cmd/backfill-deduction-student`（默认演练，加 `-apply` 执行；同名歧义一律留人工）。

| # | 原待办 | 原现状与位置 | 已实现的验收标准 |
|---|---|---|---|
| B1 | **打表记录绑定学生主键** | `model/models.go` `DeductionRecord` 只有 `student_name`/`class_name` 字符串，无 `student_id`；"写入全校学生德育档案"实为按姓名聚集，同名无法区分、转班毕业即失联 | 新增 `StudentID` 外键 + 按学生聚合的档案查询接口；老数据给出姓名+寝室+班级的回填脚本 |
| B2 | **上报与打表建立关联，修防重复扣分** | `deduction_controller.go:321` 判"已打表"只看 `building+room+今天有任意记录`，同寝室第二条上报会被误标已录入 → **漏扣分**；`InspectionPhoto` 无 `deduction_record_id` | 打表时回写关联 ID；重复扣分与漏扣分各造一条用例验证 |
| B3 | **撤销改软删除并留痕** | `DeleteDeduction` 硬删；`Status` 定义了 `revoked` 但**代码里从未赋值过** | 撤销需填理由、记录撤销人、保留原记录；导出含撤销态 |
| B4 | **补"记名纸条"这条主轨道** | 文档 3.2 的核心设计。`UploadPhoto` 只收 `room_number/photo_type/building/image`，**无文本字段、名单不落库**；而打表墙却读 `submitted_text`（那是 AI 输出）。→ 违纪学生名单目前完全靠副部长重新手打 | 上报支持"实拍/纸条/纯文本"三态；纸条可附人工填写的姓名清单；打表时从该名单选人而非手输 |
| B5 | **防冒名做成强校验闭环** | 文档第七章承诺"报假名当场识破"。现在只有前端 `alert` 出舍友列表（`app.js:3802`），打表接口不校验姓名是否属于该寝室 | B1 落地后，后端强制 `student_id ∈ room_members`，否则拒绝 |
| B6 | 名册改动的留痕 | `students` 支持 `DELETE /students/clear` 一键清空全校名册，无二次确认、无审计 | 危险操作需显式确认参数 + 审计记录 |

---

## C. 登录与会话（对应文档第二章"公共大厅"）—— **⚠️ 部分完成（2026-09-24，C1/C2/C5/C6/C7/C8/C9 已实现，34 项端到端断言全绿；C3 口令策略、C4 会话生命周期暂缓）**

落点：`middleware/auth.go`（HttpOnly 会话 Cookie `xgh_session` + CORS 白名单）、`controller/auth_controller.go`（重写）、`controller/login_guard.go`（失败限流）、`controller/audit.go`（`requireStepUp`）、`controller/role_admin.go`（角色变更专门入口 + 审计查询）、`controller/tech_db_controller.go`（role/password_hash 摘除）、`controller/deduction_controller.go` 与 `minister_controller.go`（step-up 应用点）、`static/app.js`。

顺带修复两个真 bug：① 三要素登录的角色校验原先只覆盖"手机号命中"分支，`bound_user_id` 指向非宿管账号时（种子数据恰好如此）会直接签发高权限 token——已改为对最终解析出的账号统一校验角色与手机号一致性；② 种子花名册 `BoundUserID` 硬编码为 5（指向 member_boyin 而非 dorm_ay_liu）——已改为按用户名实测查出。

**2026-09-25 补记（C1 收尾漏洞）**：`POST /api/v1/auth/logout` 缺四个角色的 Casbin 策略，除技术维护组外点"退出登录"一律 403——界面回到壁纸页但**会话 Cookie 仍然有效**。已补齐策略并实测（详见 E-1✅「本轮顺手修的洞」）。

**前提判断**：本系统最大风险不是外部攻击，而是**管理员本身是被管理者**（副部长、部长都是学生）。鉴权必须与 B 组的可归责性配套，单靠口令强度无效。

| # | 待办 | 现状与位置 |
|---|---|---|
| C1 | **改用同源 HttpOnly Cookie 会话**，弃用 `localStorage + 7 天 JWT` | `pkg/jwt/jwt.go`（密钥硬编码 `:10`、7 天无吊销）、`middleware/auth.go`。当前前端大量 `innerHTML` 注入后端字符串（`app.js:1732/1749/2072/4128`），XSS 可直接窃 token。改 Cookie 后 XSS 窃凭据通道消失，CORS 亦可整体删除 |
| C2 | **宿管三要素登录堵三个洞** | `controller/auth_controller.go`：① 楼栋 `strings.Contains` 双向模糊匹配，输入 `1` 能过 `12号楼`（`:85`）；② 按手机号命中 User 后**不校验 role** 即签发 token（`:100`）——若教师/管理员手机号被录进宿管花名册即成提权通道；③ 无失败限流 |
| C3 | 口令策略 | 全员 `123456` 且能登进生产（`repository/db.go:72`）；改密后旧 token 仍有效；首登不强制改密 |
| C4 | 会话生命周期 | 学生账号学期制流动，但账号永久有效 → 加 `expires_at`，换届即失效；提供"退出所有设备" |
| C5 | **高危操作 step-up** | 写入扣分、撤销、调他人积分、改角色，要求当场二次验证口令 |
| C6 | **拆"改数据"与"改权限"** | `controller/tech_db_controller.go:79` 通用表编辑器允许直接改 `users.role` → 拿到该会话即可自封超管。应把 `role` 摘出编辑器，改走独立授权流程 |
| C7 | 操作审计 | 全系统无 `audit_logs`。文档反复承诺"终生深度留痕"，但真正需留痕的是**操作者**。登录成败/IP/UA/打表/撤销/角色变更应只增不改 |
| C8 | CORS 收紧 | `middleware/auth.go:15-16` 目前 `Access-Control-Allow-Origin: *` 叠加 `Allow-Credentials: true` |
| C9 | 改手机号会破坏宿管绑定 | `auth_controller.go` 允许自助改 `phone`，而三要素以 phone 为主键 → 需联动校验 |

---

## D. 其余安全基线 —— **⚠️ 部分完成（2026-09-25，23 项端到端断言全绿）**

落点：`pkg/ai/client.go`（本地图转 base64 data URL 直传）、`internal/controller/url_guard.go`（SSRF 白名单）、`pkg/jwt/jwt.go`（密钥外置）、`cmd/server/main.go`（`DB_PATH`、`MaxMultipartMemory`）、`dorm_controller.go`（大小/类型校验）、`student_controller.go`（手机号脱敏）、`static/app.js`（`escapeHtml`）。

| # | 待办 | 结果 |
|---|---|---|
| D-1 | AI 图片必须公网可达 | ✅ **前提已消除**：`/uploads/...` 本地路径读出转 base64 data URL 直传视觉模型，超 4MB 拒绝。实测配好引擎后 `ai_status=real` 且上游确实收到 `data:image/png;base64,` |
| D-2 | SSRF | ✅ `validatePublicURL` 仅允许 http/https 且拒绝回环/私网/链路本地/未指定地址；网关保存时校验，探测与透传发请求前**再校验一次**（拦历史入库地址）。实测 5 类内网/非法协议全部 400，公网 https 放行 |
| D-3 | 配置层 | ✅ JWT 密钥按 `JWT_SECRET` 环境变量 > `jwt_secret.key`(0600) > 随机生成并落盘；硬编码密钥已从源码移除。DB 路径支持 `DB_PATH` |
| D-4 | 上传限制 | ✅ `MaxMultipartMemory=8MB`；宿管上传 >8MB 与非 `image/*` 一律 400 |
| D-5 | 前端转义 | ⚠️ 部分：打表台账与监控墙的后端可控字段已走 `escapeHtml`/`escapeAttr`。**全站其余 `innerHTML` 拼接点未逐一改造**，工作量大，建议后续统一处理 |
| D-6 | PII 最小化 | ✅ `/students/room-members` 对非宿管/非技术维护组脱敏手机号（实测部员见 `138****5678`、宿管见明文） |

**残留**：排班接口 `POST /minister/ai-schedule/*` 的 base64 图片仍无长度上限（D-4 只覆盖了宿管上传）。~~`model.AIConfig.APIKey` 与福利网关密钥仍明文存库（脱敏只在读取接口层，属 D-3 未做完的部分）~~ → **2026-09-26 轮 ⑤ 已闭合**：两表 `api_key` 改为 AES-GCM 密文入库、接口层只回脱敏串，启动时自动加密回写历史明文。

---

## E. 文档宣称、但代码未实现的功能（核心清单）

### E-1 完全缺失（文档承诺，代码无对应实现）

> **2026-09-25 更新**：下表中 6 行已落地（5.2 出勤加分、5.2 旷工红牌、5.2/25 寝室评优、二章素养测评、八章招新审核、3.2 记名纸条），实现位置、口径与验收证据见 **E-1✅ 本轮已落地**。「实际」列保留原始勘查结论，便于原作者复核，不作改写。

| 文档位置 | 宣称 | 实际 | 状态 |
|---|---|---|---|
| 5.2 | "干事准时完成一次排班查寝，系统**自动**记入 +5 积分" | 全库无 `attendance_ok` 写入路径，`TotalScore +=` 仅出现在代班 +3（`minister_controller.go:117`）与部长手调（`:368`）。**出勤加分不存在** | ✅ 已实现（核销式结算，见 E-1✅ ①） |
| 5.2 | "无故缺勤大盘**自动**红牌警告并扣除对应积分" | `missed` 状态只有**读取**（三色大盘 `:740`、导出、统计），无任何代码把过期未完成班次置为 `missed`，也无扣分动作。红色档永远为空 | ✅ 已实现（旷工扫描，见 E-1✅ ②） |
| 5.2 / 25 | "期末评选'**文明标兵寝室**'提供最硬核的留痕铁证" | 系统无寝室维度评分实体、无正向汇总、无评选逻辑。"文明标兵宿舍"只出现在一条**种子假新闻文本**里（`db.go:367`）。目前只有扣分流水，撑不起寝室评优 | ✅ 已实现（扣分聚合榜单 + CSV，见 E-1✅ ③，口径限制已如实标注） |
| 5.3 | "**AI 对话智能排班**" | `minister_controller.go:544` 起**未调用任何模型**，是固定模板轮流分配真实部员；含"100% 真实在册""共 N 名真实部员"等写死话术 | ✅ 已接真实调用（见「2026-09-26 晚」轮 ③：未配置即 503 不回落模板；草案仍需部长手工确认为班次） |
| 二章 | "素养常识在线测评，系统自动批改出分" | 后端 `/public/exam` 三件套 + 种子试卷**已就绪**，前端**零 UI**：无 `panel-exam`，`loadExamPaperData`(`app.js:1007`) 是死代码，首页"进入素养测评"点下去会**白屏** | ✅ 已实现（考场面板 + 判分缺陷修复，见 E-1✅ ④；**出题端仍无 API**） |
| 5.1 | "系统自动为该副部长**开通专有打表分栏、派发打表权**" | `User.Position` 是纯展示字段，`PromoteMember`(`:337`) 只改字符串，不驱动任何权限。真实权限只认 `role`（现由 B/C 的 handler 判定补齐） | ✅ 已实现（职务即授权属性，见 **2026-09-26 轮 ①**） |
| 3.2 | "记名纸条拍照，系统自动提取文字储存" | 见 B4，无文本通道 | ✅ 已在 B4 完成（三态上报 + 名单落库 + 打表选人） |
| 6.1 | 播音组新闻库维护 | `POST/PUT /publicity/broadcast/news`、`push-config` 有路由**无界面**，新闻只能靠种子数据 | ❌ 未实现 |
| 八章 | 招新后台审核 | 报名写入正常，审核列表**无 UI**（只能靠通用 DB 编辑器看表） | ✅ 已实现（部长工作台子分页，见 E-1✅ ⑤） |
| 二章 / 四章 | "移动端 APK" | `mobile-apk/` 只有 `AndroidManifest.xml` + 一份说明，**无源码、无 gradle、无 APK**。**（2026-09-25 现状已变：`mobile-apk/xgh-app/` 已有 Kotlin + Retrofit 工程，`settings.gradle.kts`/wrapper/宿管端五个 Activity 齐备，属并行会话的改动；仓库内仍无编译出的 `.apk`，本清单作者未验证其可构建性）** | ⚠️ 部分（宿管端工程已建，APK 产物与构建验证仍缺；学生/大班端未覆盖） |

### E-1✅ 本轮已落地（2026-09-25）

**落点**：`service/duty_settlement.go`（新，结算规则）、`controller/duty_controller.go`（新）、`controller/excellence_controller.go`（新）、`controller/recruit_controller.go`（补审核）、`controller/exam_controller.go`（判分修复）、`middleware/casbin.go`、`cmd/server/main.go`（6 条新路由）、`static/index.html` + `static/app.js`（三个面板 + 一个子分页）。

**① 出勤加分**：`POST /api/v1/minister/schedules/:id/complete` 把 `scheduled`/`in_progress` 班次置为 `completed`，当班部员各 **+5**，写 `member_score_logs`（`change_type=attendance_ok`）并在班次 `note` 追加核销人。部长/宿管/技术维护组可调；条件更新 + `RowsAffected` 保证**重复核销返 409**，不会重复加分。前端在排班大盘每行给「核销加分」按钮，已结算的行只显示结论不给按钮。

**② 旷工红牌**：`POST /api/v1/minister/schedules/sweep-missed[?before_date=]`（默认今天）扫描 `date < 基准日 且 status=scheduled` 的班次 → 置 `missed`、当班部员各 **-5**（`change_type=penalty`）；**该班次有 pending/approved 请假记录时改为正常销班、不扣分**（`change_type=leave`），避免把请假打成旷工。幂等，可重复执行。

**③ 寝室评优**：`GET /api/v1/deductions/room-excellence` 按 `building+room_number` 聚合**未撤销且已绑定名册主键**的扣分，`total_deduct` 升序即标兵候选；`GET .../room-excellence-csv` 导出留痕。前端独立分栏含年级筛选与四张概览卡。**无数据时不编造榜单**，直接返回说明文案。读取闸门单设 `requireExcellenceReader`（部长 / 档案导出岗 / 技术维护组），不与打表写权限混用。

**④ 素养测评**：新增 `panel-exam`（试卷网格 → 进卷作答 → 交卷出分 + 成绩报告），首页"进入素养测评"不再白屏。**判分缺陷修复**：`exam_controller.go` 原按 rune 转换题号，**题号 ≥10 时会产生控制字符**，导致多位数题号全部漏判、且 `"1"` 可能误命中他题。改为 `fmt.Sprintf("%d", q.ID)` 精确匹配后，30 题（ID 13–42）实测满分 150/150、单题 5 分无串分、脏键（空/字母/带首尾空白/越界题号）一律 0 分且不写入逐题明细。

**⑤ 招新审核**：`GET /api/v1/minister/recruit/applications`（状态/部门/关键词筛选 + 分页 + `status_stats`）与 `POST .../:id/review`（枚举校验、404、审计 `recruit.review`）。前端挂到部长工作台「招新报名审核」子分页：状态统计条可点选筛选，流转按钮按 `submitted→shortlisted→interviewed→admitted/rejected` 给出下一步，审核意见落 `interview_feedback`。

**验证方式**：独立目录 + 独立数据库 + 独立二进制，71 项后端断言全绿（越权 403、并发幂等 409、请假免罚、多位数题号回归）；前端在浏览器实机点验：以部长身份审核报名并留意见、以技术维护组身份核销班次（两名部员 100→105、105→110 且 `duty_count` +1）、执行旷工扫描（各 -5、`missed_count` +1、行标红）、空卷/单题/全对三种交卷结果、评优榜空态与有态、年级筛选、CSV 内容核对，控制台无未捕获异常。联调产生的 3 条扣分已撤销还原；两条样本班次留在**独立实例**的库里未删（仓库内的 `backend/xgh_system.db` 全程未被写入）。

**本轮顺手修的洞**：`POST /api/v1/auth/logout` 此前**只有 `tech_admin` 的 `/api/v1/*` 兜底策略能命中**，部长/部员/宿管/查看岗点"退出登录"被 Casbin 403 拦掉——界面回到壁纸页，**服务端会话 Cookie 仍然有效**（C1 引入 HttpOnly Cookie 时漏配策略）。已为四个角色补 `auth/logout` 策略，实测 403→200 且退出后原 Cookie 访问受保护接口返 401。

**仍未闭合的边界（不要在交付时声称已完整）**：
1. **无出题 API**：`/public/exam` 只有列卷、读卷、交卷三个接口，题库与试卷只能靠种子数据或通用 DB 编辑器写表。素养测评"能用"，但"学校自己出卷"这条路还没通。
2. 评优榜的口径受 B1 之前的历史数据限制——**未绑定 `student_id` 的老扣分不进入统计**（榜单说明文案已明写），需先跑 `cmd/backfill-deduction-student` 回填。
3. 出勤结算的"自动"仍是**人工触发的一次扫描**（排班大盘按钮），未接定时器；文档语义上的"到点自动"仍待一个后台任务。
4. `+5 / -5` 是写死在 `service` 常量里的规则，未做校级参数配置。**（→ 2026-09-26 轮 ③ 已把分值与额度一并挪进校级策略表）**

### E-1✅ / E-2✅ 本轮已落地（2026-09-26）

覆盖 **E-1 5.1**、**E-2 3.1**、**E-2 5.2 灵活调分**、**E-2 5.2 标兵公示**、**E-2 6.2 福利兑换前置（配置密钥明文入库）**、**F-5 前端静态资源本地化**，以及本轮实测中发现并修复的 **⑦ 通用数据库编辑器"新建用户"必然失败（附带 position 提权回退）** 共七项。落点：`model/models.go`（职务枚举与授权判定、`weekly_honor_snapshots` 快照表与 ISO 周标识、两张凭据表 `api_key` 停发并挂封装钩子）、`model/period_test.go` + `model/position_test.go` + `model/secret_hooks_test.go`（新增单测）、`pkg/secretbox/`（新，含单测）、`service/score_policy.go`（新）、`controller/score_policy_controller.go`（新）、`controller/honor_controller.go`（新）、`controller/duty_controller.go`、`controller/minister_controller.go`、`controller/deduction_controller.go`、`controller/publicity_controller.go`、`controller/export_controller.go`、`controller/tech_db_controller.go`、`controller/tech_controller.go`（脱敏视图与专用请求体）、`controller/welfare_controller.go`（掩码与写库错误处理）、`repository/db.go`（启动回写历史明文）、`static/index.html` + `static/app.js`（技术组「积分策略与调分复核」分栏、部长工作台「每周标兵公示」看板、AI 与福利网关的脱敏回显）、`static/vendor/`（新，Tailwind Play 运行时 + Font Awesome 6.4.0 全量图标字体 + 两款正文/等宽字体的 latin 子集）、`static/style.css`（Google Fonts `@import` 改为本地）。

**① 任免联动打表权限（E-1 5.1）**：`User.Position` 不再是纯展示字段。新增 `model.PositionMember/PositionVice/PositionMinister` 与 `model.HasDeductionAuthority(user)`——打表授权的**唯一口径**（技术维护组恒可用；部员/部长须同时满足"部门含技术 + 职务副部长"）。`requireDeductionAuthority` 与 `PromoteMember` 共用该判定，因此部长任命技术部副部长即**派发**打表权、降回部员即**回收**，服务端每次请求重读数据库，**无需重新登录即生效**。`PromoteMember` 同时补齐：职务枚举校验（枚举外值与"部长"一律 400，部长只能由技术维护组变更角色产生）、禁止任命自己、禁止部长调整非部员（宿管/部长/查看岗）、重复调整 400、响应回传 `authority.{before,after,granted,revoked}` 与提示语、审计留痕写明"并派发/回收打表权限"。前端 `hasDeductionAuthority()` 镜像同一口径并补上部长分支的打表入口，启动与标签页切回时 `syncIdentityFromServer()` 调 `/auth/profile` 覆盖本地缓存并重绘侧栏（被回收者若正停在打表面板会自动离开）。**顺手堵的洞**：通用数据库编辑器 `PUT /tech/db/tables/users/:id` 原先可直写 `position`，等于绕开 step-up 与留痕造出打表权，现与 `role`/`password_hash` 一并剔除。**（当时只堵了 PUT 这一半，同一编辑器的 `POST` 新建路径仍能注入 `position`，且 role 的限制与表单 schema 互斥导致新建必然失败——属本末倒置的半拉子改动，由同轮 ⑦ 闭合。）**

**② 时段任务卡片枚举统一（E-2 3.1）**：`PeriodType` 三处不一致已收敛为一个枚举 `daily/weekday/weekend/single_week/double_week`，历史复数写法（`weekdays`/`weekends`）读取时归一、写入时严格（非法值 400，含 POST 与 PUT 两条路径），并有一次性数据规范化。周末、周内工作日、单双周判定真正生效——**单双周按 ISO 周奇偶判定，UI 已标注可能与校历相差一周**，需以校历为准时由技术维护组核对。

**③ 灵活调分收口（E-2 5.2）**：新增校级策略表 `score_policy_configs`（单行，缺省即返回默认值 5/5/10/20/5，不预置种子），出勤加分、旷工扣分、单次调分上限、同一部员七日累计额度、复核线全部可配；`AdjustScore` 校验理由（4–120 字）、单次上限、七日滚动额度、角色与同部门约束，命中复核线的流水标记 `needs_review`。技术维护组新增 `GET/PUT /minister/score-policy`、`GET /minister/score-adjustments`（近 N 天清单，可按部门与"仅看待复核"过滤）、`POST /minister/score-logs/:id/reverse`（**冲正**：追加 `manual_reversal` 反向流水并退回积分，原流水不删、同一笔只能冲正一次，重复冲正 409）。策略保存与冲正均需 step-up 口令。技术组控制台的第四个分栏即该面板。

**④ 每周标兵公示快照（E-2 5.2）**：新增 `weekly_honor_snapshots` 表（`week_key + scope + rank_type` 唯一）与 `model.WeekKeyOf()`，公示从"请求时实时算"改为**评定一次留一档**。`POST /minister/honors/evaluate` 由部长（恒锁本部，请求参数里的 `department` 一律忽略）或技术维护组（可指定部门或全校）发起，按 ISO 周区间统计该范围在册部员的上岗班次与缺工流水，产出「最高积分」「最优上工」两档快照，同周重复评定**覆盖本周**而不堆叠，`note` 里写清评选口径与并列取舍规则（缺工更少 → 上岗更多 → 账号更早），审计动作 `honor.evaluate` 记录评定人。`GET /minister/honors/weekly` 与部员端 `GET /member/honors/weekly` **只读快照**：未评定时如实返回 `evaluated: false` 与空数组，绝不回落到实时榜首冒充公示结果；`GET /minister/honors/history` 按周倒序回溯往期。尚未开始的周不可评定（400），周标识格式与范围校验（400）。部长工作台的原两张标兵卡片改由快照驱动，并补周次徽标、评定人/评定时间、公示留痕与「评定本周标兵」二次确认。

**顺手修掉的公示计分错误**：`publicity_controller.go:166` 的红黑榜积分从流水表聚合了一个**不存在的 `points` 列**（字段实为 `score_change`），GORM 报错被 `.Scan` 吞掉，人人积分恒为 0，于是 `TotalScore < 100` 的黑榜判定把**每一位部员**都判进黑榜；`export_controller.go:260`、`:374` 两处 CSV 同源同错，台账里"考核总积分"恒 0、荣誉档次恒降为最低档。三处统一改读权威余额 `users.total_score`（出勤结算、代班、灵活调分每条流水都同步过它），不再做二次聚合。

**⑤ 配置密钥密文入库与脱敏回传（E-2 6.2 前置）**：新增 `pkg/secretbox`（AES-256-GCM，落库形态 `enc:v1:<base64>`；密钥解析顺序与 `pkg/jwt` 同约定：`CRYPTO_SECRET` 环境变量 > `crypto_secret.key`(0600) > 随机生成并落盘，密钥不进源码）。`AIConfig.api_key` 与 `TechWelfareGateway.api_key` 标 `json:"-"`——**响应体里从此不存在这个字段**，明文不会再出现在前端状态、浏览器历史、反向代理日志或截图里；封装/还原挂在 GORM `BeforeSave`/`AfterFind` 上，业务侧与上游调用（AI 双引擎、福利透传中继）拿到的仍是明文。启动时 `sealLegacyAPISecrets` 用 `Table()` 绕开钩子读原始列，把升级前已明文入库的记录逐条加密回写（独立实例首轮日志：`ai_configs 已将 1 条历史明文密钥加密回写`）。读取接口只回 `has_key` + 掩码（首 4 `****` 尾 4）+ 服务端判定的 `configured`；`PUT /tech/ai-configs/:id` 的 `api_key` 传空即沿用原值，审计动作 `tech.ai_config.update` 只写"已替换密钥（密文入库，不记录密钥内容）"。福利网关保存/更新此前**不检查写库错误**，封装失败也会回"成功"，现改为 500 并说明"未生效"；解密失败（换过 `crypto_secret.key`）时 AI 配置读取返回 500 与可执行提示，不再被当成"库里没有配置"。前端 AI 卡片改按 `configured` 出徽标、按 `has_key`/`api_key_mask` 出占位提示，福利网关编辑弹窗同样只显示脱敏串并保持"留空 = 不改动"语义。**本轮修出的自身回归**：`UpdateAIConfig` 原先直接以 `model.AIConfig` 绑定请求体，字段标了 `json:"-"` 之后入参密钥被 encoding/json 静默丢弃，**新密钥根本存不进去**（独立实例首轮 29/38：拷库读原始列的 `B4/B6` 两项直接暴露列值仍是空串，其余失败是测试脚本自己取错响应字段名），已改为只用于写入的专用请求结构体——"只进不出"的模型不能同时当入参 DTO 用。

**⑥ 前端静态资源本地化（F-5）**：`index.html` 原先引 `cdn.tailwindcss.com` 与 cdnjs 的 Font Awesome CSS，`style.css:5` 还有一条 `fonts.googleapis.com` 的 `@import`。三处已全部改为同源本地文件，新增 `static/vendor/` 共 13 个文件、1.45 MB：`tailwind-play-3.4.17.js`（407,279 B；原 URL 不带版本号、实际版本由 CDN 决定，现固定在 3.4.17）、`fontawesome/css/all.min.css`（102,025 B）与 8 个 `webfonts`（solid/regular/brands/v4compatibility 各 woff2+ttf，woff2 合计 287,656 B）、`fonts.css`（3,272 B、8 条 `@font-face`）与 `plus-jakarta-sans-latin.woff2`(27,348 B)、`jetbrains-mono-latin.woff2`(31,432 B)。**那条 `@import` 比外链字体本身更要命**：CSS 的 `@import` 阻塞其后的样式表解析，网络受限时整份 `style.css` 一起被拖住。改后 `static/` 下已不存在任何外链 `src`/`href`；剩余外链只有 `app.js:79-85` 的 7 张 Unsplash 主页壁纸与 `:1657` 素材工坊图片的 `onerror` 兜底串，属业务图源而非渲染依赖（断网时表现为壁纸与图片列表空白，界面框架不受影响）。**顺带做了部署侧交叉核对**：另一会话新增的 `backend/Dockerfile` 含 `COPY static ./static`，且 `backend/.dockerignore` 未排除 `static/vendor/`，故容器镜像会带上这批本地资源——本地化只有打进部署产物才算数。

**关于 Tailwind 必须说清的残留**：本轮消除了"依赖公网"，但**没有改变渲染方式**——页面仍在浏览器里用 Play 脚本做运行时 JIT 生成样式。该包内 `console.warn("cdn.tailwindcss.com should not be used in production...")` 是一条**无条件的顶层语句**，本地化后依旧打印；它提醒的是"别用运行时 JIT"，不是"别用 CDN 域名"。要彻底消除需在构建期预编译一份静态 CSS，而本项目的既有约定是**前端无构建步骤**（`static/` 由 Gin 直接托管），故未擅自引入 node 构建链，列为后续可选项。

**⑦ 通用数据编辑器新建账号修复（使用方 2026-09-26 报出）**：现象是「向【系统用户与干事部员名单】录入新记录」一律返回"通用数据编辑器不得指定账号角色，请改用 POST /tech/users/:id/role"。根因是 ① 那轮改动**只做了一半且自相矛盾**：`tech_db_controller.go` 的表 schema 仍把 `role` 声明为 `type: select, required: true`（新增弹窗因此强制要求选一个角色），而创建处理又规定 `role` 非空即 403——留空则被前端 `required` 挡住，**没有任何输入能通过**，该入口从改动当天起就是死的。修法是把"权限属性不接受通用编辑器入参"这条语义在两端对齐：`ColumnMeta` 新增 `ReadOnly` 字段并标给 `role`（同时给列表补一列只读的"现任职务"，让打表授权属性看得见但改不动），前端 `renderDBFormFields` 遇只读列只渲染静态虚线框、不渲染控件，提交体因此天然不含该字段；后端创建路径**去掉 403**，改为无条件 `role = member`、`position = 部员`，与该表更新路径早已存在的静默剔除语义一致。另补两处入参处理：`username`/`real_name` 为空返回 400；账号重复改为"先查再插 + 回『登录账号 X 已存在』"，此前会撞唯一索引、把 sqlite 原始错误以 500 抛给前端。**顺带堵掉一个真实提权洞**：创建路径是 `ShouldBindJSON(&model.User)` 整行绑定，只中和了 `role` 与 `password_hash`，**`position` 从未剔除**——具 tech 权限者直接 `POST /tech/db/tables/users` 提交 `{"department":"组织部 · 技术组","position":"副部长"}` 即可造出满足 `HasDeductionAuthority` 的打表账号，绕开 step-up 与任免留痕（UI 里没有 position 这一列，从页面上点不出来，但接口是敞的）。

**验证方式**：独立目录 + 独立数据库 + 独立实例（本轮以 `go run ./cmd/server` 运行同一套源码，原因见下"本机环境限制"）。`go vet ./internal/... ./cmd/... ./pkg/...` 与 `go test ./internal/... ./pkg/...` 全绿（含新增 `position_test`、`period_test`、`secretbox` 单测、`internal/model/secret_hooks_test.go`——后者直接断言"库里那列是密文、GORM 读回来是明文、反复保存不会层层加壳、空密钥不会被写成空密文"）；后端断言 **17（3.1 时段）+ 21（5.2 调分与复核）+ 34（5.1 任免与权限联动）+ 34（5.2 标兵公示快照与红黑榜计分）+ 38（6.2 密钥密文入库与脱敏回传，重复跑两轮）+ 28（⑦ 通用编辑器新建与提权回退，连跑两轮）** 全绿。⑤ 的关键断言是**绕开应用层直接拷库读原始列**：写入的明文密钥在整个 `.db` 文件二进制里扫描不到、列值以 `enc:v1:` 开头且标记只出现一次、`api_key` 传空保存后掩码不变（证明确实沿用原值）、响应 JSON 里根本不存在 `api_key` 字段、部员与宿管读取 AI 配置一律 403、审计明细不含密钥内容。另实测**密钥不一致时的降级**：用另一把 `CRYPTO_SECRET` 启动后读 AI 配置返回 500 且提示核对 `crypto_secret.key`（不是假装配置为空），福利网关列表退化为 `****` 且不外泄密文。④ 的关键一条：**评定后给该标兵临时 +2 分，实时台账立即变成 115，已公示快照仍是 113**——这才证明公示不再是实时数据的别名。浏览器实机点验以 `evaluate_script` 读 DOM：策略表单读写、调分清单渲染与"已冲正/待复核"标记、任免后被任命者侧栏自动出现打表入口、回收后入口自动消失且面板自动跳回部员页、标兵看板按快照填充周次/姓名/分值/称号/口径说明并列出两期留痕、点「评定本周标兵」后评定时间刷新且同周仍只有两档、AI 两张引擎卡片显示"已配置可用"且密钥框为空只带 `已存密钥 sk-E****y-RR：留空 = 保留原值` 占位、福利网关编辑弹窗占位显示同一掩码而新建弹窗复位为提示语；未评定的空态用**服务端真实返回的 `evaluated:false` 报文**驱动渲染（卡片显示"待评定"、说明区显示服务端原文、留痕区清空），因该看板不提供部门选择器，无法在真实已评定范围内自然复现空态，故此处为报文级驱动而非真实未评定账号目视。控制台无未捕获异常；仍会打印一条 `cdn.tailwindcss.com should not be used in production`，它来自 Play 脚本包内那条**无条件的顶层语句**（见上"关于 Tailwind 必须说清的残留"），不代表页面还在访问公网。⑥ 的验证同样以浏览器实机读数为准：整页 13 个资源中**只有 1 个跨域**（一张 Unsplash 壁纸），三份样式表分别来自 `/static/style.css`（249 条规则）、`/static/vendor/fontawesome/css/all.min.css`（1938 条）与 Tailwind JIT 注入的 `<style>`（430 条），说明本地 Tailwind 运行时确在生成样式；`document.fonts.check('900 16px "Font Awesome 6 Free"')` 为 true 且图标元素的计算 `font-family` 即该家族；两款正文字体首帧显示 `unloaded` 是浏览器懒加载（工作台模式下 `body`/`.font-mono` 被 `body.console-mode` 的系统字体栈覆盖，只有主页落地页会真正取用），显式 `document.fonts.load()` 后各自从 `/static/vendor/fonts/*.woff2` 取到文件，且同一段 64px 试排文本宽度 Jakarta 531.53px vs 兜底字体 515.44px、JetBrains Mono 576px（严格等宽）vs Arial 483.78px——即本地字体是真的生效而非空挂。⑦ 的回归是 `e2e_dbusers.py` **28 条断言、独立实例连跑两轮全绿**：schema 侧断言 `role` 已变 `read_only` 且不再必填、新增 `position` 只读列、其它表定义不受影响；新建侧断言只填业务字段即 200、落库角色恒 `member`、职务恒 `部员`、响应无 `password_hash`、新账号确实出现在名单里；**提权侧是故意在提交体里塞 `"role":"tech_admin","position":"副部长","department":"组织部 · 技术组"`**，断言结果仍被固定为部员，并用该账号真实登录——`POST /deductions` 得 403、`GET /tech/db/tables` 得 403，即确实没拿到打表权也没拿到技术组接口；校验侧断言空账号 400、重复账号 400 且提示文案里不含 `UNIQUE`/sqlite 原文；不回退侧断言 PUT 仍吞掉 `role`/`position` 而 `real_name`/`total_score` 照常改得动、部员调写入接口仍 403。浏览器实机走完整 UI 流程：新增弹窗里 `role`/`position` 渲染为静态虚线框"（新建时由服务端指定）"、可编辑控件只剩 8 个业务字段且仅 2 项必填，真实提交后弹"数据记录已成功入库！"、列表出现 `#24 zz_browser_ui 浏览器实测账号 member 部员 纪检部 … 100 active`，重开编辑弹窗两静态框显示 `member`/`部员`（证明只读列在编辑态也不会被回写成输入框），最后用页面删除按钮清掉该账号（删除确认框以 `window.confirm` 桩替代，属操作手法而非行为差异）。截图工具在本机不可用（`NATIVE_BROWSER_VIEWPORT_UNAVAILABLE`），故视觉呈现未经像素级确认。**本机环境限制**：360 杀毒会把新构建出的 `xgh_gov.exe` 判为可疑并删除/锁定（连读取都返回 Permission denied），故本轮独立实例改用 `go run ./cmd/server` 起服务（`go test` 二进制不受影响）——这是本机杀软对未签名新 exes 的误报，与代码无关，Linux 部署无此问题。测试期间被临时改动的部门、职务、积分策略与标兵调分均已还原；⑦ 建的三个测试账号（两个走 API、一个走浏览器）已全部删除，名单里不残留 `zz_` 前缀记录；仓库内的 `backend/xgh_system.db` 全程未被写入（⑤ 的密钥改写只发生在独立实例的库与配置行上）。

**本轮发现但未修（不要声称已完整）**：
1. **`GenerateSchedule` 仍不认单双周与周末**：`minister_controller.go` 生成排班时只有 `rule_type == "weekday"` 一个分支生效，`weekly_single_double` 不会真正按周交替、也没有 `weekend` 分支。②修的是**时段卡片的提交窗口判定**，排班生成引擎属另一处遗留，未纳入本轮范围。
2. 标兵**评定仍需人工点按钮**：快照与留痕已落地，但文档"每周**自动**评定并公示"里的定时触发还没接（与出勤结算同一处遗留——缺一个后台定时器）。
3. `/public/exam` 仍无出题 API。
4. ⑤ 只做**静态加密**，没有**密钥轮换**：换掉 `CRYPTO_SECRET`/`crypto_secret.key` 后已入库的密文无法解密（读取会明确报 500 而不是静默出错），也没有"旧钥解 → 新钥封"的重封装接口。因此 `crypto_secret.key` **必须与数据库文件同批备份**，丢了它等于所有已配密钥要人工重录。另：`ai_configs` 与 `tech_welfare_gateways` **不在**通用数据库编辑器 `GET/PUT /tech/db/tables/*` 的 8 张表白名单内，该编辑器不构成明文旁路。
5. ⑥ 只做到**同源化**，两处残留：① Tailwind 仍是浏览器端运行时 JIT——约 400 KB 脚本参与首屏解析，样式在 JS 跑完后才出现（弱网/低端机上有可感知的"先裸后花"），预编译成一份静态 CSS 才能根治，但那会第一次引入前端构建链，需项目owner同意；② 主页 7 张壁纸与素材工坊图片仍是 Unsplash 外链且**没有本地兜底图**，断网时表现为空白而非降级占位——不影响可用，但上线前建议换成本地内置图或允许配置为纯本地模式。

### 本轮已落地（2026-09-26 晚，使用方报出 2 个 BUG + 要求 2 项 AI 界面）

覆盖 **① AI 引擎双卡片保存互相覆盖（BUG）**、**② 导出中心已登录仍提示需登录（BUG）**、**③ 高性能 AI 模型透传交互终端 + 前端"模拟 AI 聊天"改为真实调用（E-1 5.3 / E-2 6.2）**、**④ 宿管终端 AI 识别结果的人工纠正编辑框（E-2 4.2 补强）**，以及实测中发现、经使用方定口径后闭合的 **⑤ 全校违纪台账整表导出收紧** 共五项。落点：`static/app.js`（下载统一走 `downloadAuthedFile`、AI 卡片单点重绘、透传终端真实对话、宿管纠正表单、部长 AI 排班对话去模拟化与转义）、`static/index.html`（`#dorm-fix-box` 纠正卡片与状态徽标）、`pkg/ai/client.go`（`firstLineOf` 改按 rune 截断，上游中文错误页不再被砍出半个字）、`controller/minister_controller.go`（`ChatAISchedule` 接真实文本引擎 + 图片来源先做入参分类）、`controller/welfare_controller.go`（`RelayChat` 真实透传、余额预检、原子扣减）、`controller/deduction_controller.go`（新增 `requireDeductionLedgerReader` 与导出查询错误处理）、`controller/dorm_controller.go` + `cmd/server/main.go`（新增 `POST /dorm/inspections/:id/correct`）、新增 `pkg/ai/client_chat_test.go`、`internal/controller/welfare_quota_test.go` 与 `internal/controller/deduction_ledger_reader_test.go`。

**① AI 引擎双卡片保存互相覆盖**：根因**不在服务端**——`PUT /tech/ai-configs/:id` 本就是单行全量替换，与另一张卡片无关；是前端保存成功后把整块卡片网格重新 `innerHTML` 渲染了一遍，左右两栏因此都按服务端旧值重建，**另一张卡片尚未点击保存的输入被清空覆盖**。修法是最小化重绘范围：`aiEngineCard(c)` 的卡片根节点带 `id="ai-engine-card-${c.id}"`，保存成功后只用手中的响应报文替换**那一张**卡片（`replaceAIEngineCard`），兄弟节点不重建，其未提交的输入原样留在 DOM 里。

**② 导出中心已登录仍提示需登录**：这是 **C1 的收尾没做完**。四个导出按钮此前既保留了 `if (!state.token)` 的本地门禁，又手工拼 `Authorization: Bearer ${state.token}`；而 C1 之后 Web 端会话已改为 HttpOnly Cookie，`state.token` 恒为 `undefined`——偏偏 `middleware/auth.go:87` 是**只要看到 Bearer 头就优先于 Cookie**，于是这个头不是"没带凭证"而是**主动把已登录的会话打成无效**，接口回 401、前端按老逻辑提示"请登录"。修法是新增 `downloadAuthedFile(url, filename, failLabel)`：纯 Cookie `fetch`，401 提示"登录已失效，请重新登录后再导出"，非 200 时解析服务端 JSON 的 `error` 字段，成功才用 blob + anchor 落盘；六个下载点（ZIP 多合一、每日/常驻值班 CSV、标兵寝室 CSV、违纪台账 CSV、园区档案 CSV）全部改用，删除所有本地 token 门禁与手工 Bearer 头。**权限并未放宽**：Casbin 早已把 `/export/*` 限定给 `viewer_export` 与 `tech_admin`，客户端那道门禁从来不是防线。**顺带堵掉的静默失败**：原实现把响应体无脑存成 `.csv`，401/403 时用户会拿到一个内容是 JSON 错误报文的"表格文件"且毫无提示。

**③ 真实 AI 透传终端 + 前端两处"模拟聊天"改为真实调用**：
- **透传终端**（`POST /welfare/chat-relay`）：原先前端是本地假应答，现改为真实发起调用并如实呈现结果。服务端 `RelayChat` 的口径：先校验 prompt（≤4000 rune）、网关启用、有密钥、模型可解析，`resolveCostPerCall` **按调用者本部门定价**（缺省回落全局行，禁用行不参与），上游地址过 `validatePublicURL`（SSRF 前置），system 提示 + **最近 10 轮合法历史** + 本轮 prompt 一起发出；**扣次数前先 dry-run 余额预检**，不足直接 403 并指出去哪里兑换；上游失败返回 502（`ErrNotConfigured` 为 400）+ `ai_status:"failed"` + **既不计入次数也不落一条回复**，成功才 `tryChargeQuota` 原子扣减，扣减失败则 500 且**丢弃已拿到的回复**（不出现"扣了次数没给答案"或"给了答案没扣次数"）。审计 `logRelay` 只记通道/模型/耗时/结果，**不记 prompt 正文**。
- **部长 AI 对话排班**（`POST /minister/ai-schedule/chat`）：HEAD 版本 `grep ai.` 为空——**一次模型都不调**，用固定模板轮流分配真实部员却声称"AI 排班"。现改为真实调用文本引擎，并保留原有的两道硬约束：产出的姓名必须命中在册部员（名册外一律剔除）、日期必须落在排班窗口内（超窗丢弃），响应附【服务端真实核验】一行说明实际保留/剔除了什么。文本引擎未配置时 **503 + `ai_status:"disabled"` 并直接说明去哪配置**，绝不回落成模板冒充模型输出。前端另补一条客户端入参分类：随附图片既不是 `data:image/`、也不是本地 `/` 或 `http(s)` 时直接 400 提示重新选图，不再把它送给上游换回一个语义模糊的 502。
- **反 HTML 注入**：两处聊天窗自此把**远端可控文本**渲染进 DOM，用户消息、助手回复、失败原因一律 `escapeHtml`；注入探针以 `<img onerror>` 提交后 DOM 中 IMG 节点数为 0。
- **前端失败态与账务一致性**：本轮调用失败时**不写入历史**（否则一次失败会污染后续上下文）并弹红色气泡"本轮调用失败（未扣除次数）"；余额字段原读 `data.remain_quota`（**响应里根本没有这个键**，所以界面永远不刷新剩余次数），改为 `data.remain_calls`。

**④ 宿管 AI 识别结果的人工纠正**：新增 `POST /api/v1/dorm/inspections/:id/correct`。宿管终端上传后，识别完成即在同一面板展开纠正卡片，预填视觉文本、隐患类别、严重度、扣分、摘要、建议，徽标从"待宿管核对"起。保存时**只提交与预填值不同的字段**并**强制填写纠正理由**（≤500 rune），扣分限 0–30、严重度限枚举，无差异 400；服务端把每一处改动写成人类可读的"`归纳摘要：A → B`"落 `review_note`（**只追加不覆盖**，这是该字段第一次被真正使用），状态置 `manual_corrected`，审计动作 `dorm.inspection.correct`。权限：仅该条记录的**上报人本人**可纠正（技术维护组例外），非本人 403；已转扣分（`converted`）的记录 **409 拒绝**——改一条已经变成扣分流水的识别结论必须走撤销通道。**关键不变量：纠正不会改写 `ai_status`**，否则人工改一次就能把一条 `disabled`/`failed` 的记录洗成"AI 已核验"，绕过 `deduction_controller.go` 打表处的 `aiVerified` 闸门。

**验证方式**：独立实例（`go run ./cmd/server` + 独立库）后端断言 **57 条全绿**，覆盖：四个导出在纯 Cookie 下 200 且字节数正常、带 `Bearer undefined` 与不带 Cookie 均 401、部员访问导出 403；AI 配置 PUT 返回 `config` 且兄弟配置行的 `has_key` 不变；透传终端矩阵（不可解析端点在保存时即拒、上游失败 502 且响应无 `reply`、`ai_status=failed`、`total_relayed_calls` 不变、余额不足 403、兑换成功、**失败不扣部员次数**、历史被接受、>4000 rune 400、私网端点拒、审计落库且**不含 prompt 正文**）；排班 AI 在 502/503 下均**不产出任何草案**、关掉文本引擎后 503 `disabled`；纠正接口 happy path + 五种拒绝 + 归属 + 404 + `converted` 409 + 审计。新增单测：`pkg/ai/client_chat_test.go` **11 个用例/子用例**（多轮请求真实携带 history 与 `Bearer`、`max_tokens=0` 时不下发该键、**8 种上游异常一律返回空字符串而不编造**、未配置与失败的区分、超大响应体拒绝、任意字符串不当图片送上游）、`internal/controller/welfare_quota_test.go` 4 个用例（**并发 8 个请求抢 10 次余额只成功 1 次**、同池 all-or-nothing、旧网关池仍可扣、定价取调用者本部门）。`go vet ./...` + `go test ./internal/... ./pkg/...` 全绿。浏览器实机（`evaluate_script` 读 DOM + 真实点击）：保存左卡后右卡未提交的输入**逐字保留且节点未被重建**；四个导出真实下载得到 1705/15863/1965/2138 字节且**无"请登录"提示**；纠正卡片保存后回"已纠正 3 项"且 `severity` 以 `critical` 落库；透传终端显示红色失败气泡并带真实拨号错误。**本轮修出的自身 BUG**：`dormFixValue()` 返回的是字符串，而我按 `dormFixValue("dorm-fix-severity").value` 取值 → `undefined` → JSON 序列化时**静默丢键**，表现为界面报"已纠正"而严重度从未改成功——只有驱动浏览器才暴露得出来。

**⑤ 全校违纪台账整表导出收紧（使用方定口径后落地）**：上面第 1 条洞已按「**保留公示可读、只收紧整表导出**」的口径闭合。新增 `requireDeductionLedgerReader`（`deduction_controller.go`，紧邻 `requireDeductionAuthority`），`ExportDeductionsCSV` 入口调用：放行**档案导出岗、部长、技术维护组、持打表权的技术部副部长**（复用 `model.HasDeductionAuthority`，身份取数据库最新值，任免即时生效），其余角色 403 且响应明确写出可读范围；停用账号 403、账号已不存在 401（不当成"有权限"）。`GET /deductions` 分页列表**有意保持对全体登录用户开放**——部员端要看违纪公示，而同一文件的按学生档案查询（`ListStudentDeductionProfiles` / `GetStudentDeductionProfile`）此前已有 `requireDeductionAuthority`，所以"列表可看、整表不可拉走"是自洽的口径。**顺带修掉该处理器的静默失败**：查询错误原先被忽略，使用者会拿到一份只有表头的"空台账"，现改为写出任何 CSV 字节之前返回 500 并说明"导出未执行"。前端无需改动——该按钮本就只在 `panel-deductions`（打表面板，前端已按打表权渲染）内，且 `downloadAuthedFile` 会把 403 的 JSON `error` 原样 toast 出来。Casbin 侧未动：这是第二层属性判定，与既有分层一致。**验证**：新增 `internal/controller/deduction_ledger_reader_test.go` **6 个用例**（四类放行 + 四类拒绝 + 停用/账号缺失 + **任命即放行、免职即回收** + 处理器级 403/200 与 BOM/筛选仍生效 + 查库失败不写出半个 CSV 字节）全绿；`go build ./...`、`go vet ./...`、`go test ./...` 全绿（controller/model/service/pkg/ai/pkg/secretbox 五包）。**未做**：导出动作本身**尚未写审计**（其余导出亦未记，属同一遗留），若要"谁在何时拉走全校名册"可追溯，需要在该处理器补一条 `logOperationAs`。

**本轮发现但未修（不要声称已完整）**：
1. ~~`member` 角色可读可导出全校违纪名册~~ → **已按使用方确认的口径收紧，见上 ⑤**。仍存的相邻事实：`GET /api/v1/deductions` 分页列表对全体登录用户可读（**这是刻意保留的公示口径**，如校方认为违纪明细不应向普通部员公开，需另做一轮按部门收敛）。
2. **宿管纠正未接 step-up 口令**： mitigation 是理由必填 + 仅限本人 + 0–30 上限 + 审计 + 不可改已转扣分记录；若要与其他高危操作对齐需加 `requireStepUp`，但宿管端是否应有"口令"概念是产品设计决定，先不引入。
3. `/welfare/exchange*` 两条兑换路径的**定价读取与扣积分仍非原子**（与本轮修的透传扣次数不是一条代码路径），并发下理论上可重复兑换；属既有实现，本轮只做了 `tryChargeQuota`。
4. 透传终端"端到端真扣一次上游"仍**未用真实密钥验证**：本机 DNS 被污染且无公网可达的可用端点，成功路径靠 `pkg/ai` 的 httptest 桩覆盖，账务与失败态靠 HTTP 断言覆盖。
5. 5.3 排班草案的**落库通道未变**：AI 出的是草案，部长仍需手工确认为班次，未接"一键采纳"。

### 本轮已落地（2026-09-26 深夜，宿管留痕详情 + 内存溢出 + API 文档重核）

覆盖 **① 宿管终端"所辖楼栋历史拍照留痕"卡片可点击查看详细信息**、**② 无法连接后端时前端内存溢出的根因定位与修复**、**③《API接口文档.md》整体重核** 共三项。落点：`controller/dorm_controller.go`（新增 `GetInspectionDetail` 与 `LinkedDeductionBrief`）、`cmd/server/main.go`（注册 `GET /dorm/inspections/:id`）、`static/app.js`（`guardBrokenImages` + 内联 SVG 占位、卡片可点击化与转义、`openInspectionDetail`/`renderInspectionDetail` 详情渲染）、`static/index.html`（`modal-inspection-detail` 详情弹窗骨架）、新增 `internal/controller/dorm_inspection_detail_test.go`（6 用例 + 5 子用例）、仓库根 `API接口文档.md`（9 处更新）。

**① 留痕卡片可点击查看详细信息**：新增 `GET /api/v1/dorm/inspections/:id`，一次返回记录本体 + 记名名单 + 已关联打表 + 结构化归纳。**这里唯一真正需要小心的是越权面**：宿管在 Casbin 上持有 `/api/v1/dorm/*` 的 GET|POST 通配（`middleware/casbin.go:41`），所以"列表接口本身只返回本楼栋"**并不构成**对按 id 寻址路由的任何约束——详情处理器必须把列表里那条楼栋过滤**逐字重放**一遍（`building LIKE '%X%'`，`全楼` 与技术维护组不受限），否则通配直接变成一个绕过列表口径的读放大器。两处口径不一致就是漏洞，因此过滤语句不是"语义等价"而是同一写法。**不存在与越权合并成同一个 404 且报文逐字相同**——否则"这个 id 存在但不给你看"会把详情接口变成全校留痕记录的存在性探测器。编号非法（`abc`、`0`、`-3`、空串、纯空白、`1e99999`、`<id>x`）在解析阶段就 400，不落到查库；`structured_json` 是宿管可影响的自由文本，空串/半截 JSON/非对象一律降级为 `nil` 且**不影响本接口成功**（归纳结论缺失不该让整条记录看不见）。已关联打表走 `LinkedDeductionBrief` 而不是整行 `DeductionRecord`，只暴露分值/类别/状态/操作人这类核对所需字段，`reason` 不外传。前端：卡片带 `role="button"`/`tabindex="0"`/`onclick` 并补 Enter 与 Space 键盘触发（只有鼠标可用对键盘用户等于没有这功能），`id` 显式 `Number()` 化，所有远端可控字段过 `escapeHtml`，弹窗沿用页面既有 modal 约定。

**② 无法连接后端时内存溢出**：根因是一处 `<img>` 的 `onerror` 里**重新赋值 `src`**——图片指向已失效的本地 `/uploads/...`，`onerror` 又把 `src` 换成一张远程兜底图，远程同样取不到时再次触发 `onerror`，形成**无界自循环**。实测在 4 秒内从 2 张图收到 **8081 次 error 事件**（约每张每秒 4000 次），每一轮都重新排一次请求与解码并压一层回调，页面内存因此被打满——这正是使用方所述"连不上后端时内存溢出"。**修法不是加防抖，而是把这条路径上的网络依赖彻底摘掉**：`guardBrokenImages(container)` 在容器上装**一次性**捕获阶段监听（`dataset.imgGuarded` 保证重复调用不叠加），命中后先 `img.onerror = null` 掐断自循环，再把 `src` 换成一份**零网络依赖的内联 `data:image/svg+xml` 占位图**（"图片不可用"），因此不存在"兜底图自己也需要网络"的次生失败。同一容器内重测 4 秒仅 3 次事件。**必须说清一件事**：第一次在独立实例上实测时 error 计数是 **0**，我原本准备直接宣称机制成立——去查了 img 实际状态才发现这台机器能正常出公网，兜底图真的加载成功了，循环自然自终止。也就是说**"没复现"不等于"假设错了"**，改用同引擎确定性夹具才把 8081 这个数字证出来，这一点比结论本身更值得记。顺带把缩略图改成 `loading="lazy" decoding="async"`，空态补显式文案而非空白网格。

**③《API接口文档.md》整体重核**：文档此前已明显落后于工作树（**README 与本文都属"审计当时"快照，而代码这两周改了十几轮**）。本轮以**运行中服务真实打印的路由注册日志**为准统计对外端点数（**104 条**），并逐条回到源码或现场 HTTP 复核，共 9 处更新：文首加本轮重核范围说明；§1 新增"导出的鉴权方式"一行并点明 `middleware/auth.go:87` 那条"看到 Bearer 头就优先于 Cookie"的语义（上一轮 ② 号 BUG 的根因，文档原先完全没写）；§2.2 权限矩阵五个角色全部重列；新增 §2.4 `requireDeductionLedgerReader`（上一轮 ⑤ 的整表导出口径）；§4.1 修正 `ai_status` 三态说明并补两个新端点小节；§4.2 导出条目改指向 §2.4；§5、§6、§7 分别把排班 AI 对话、AI 引擎配置、福利网关与透传终端的现状改写为真实行为；§8 契约变更表补两行；§9.6/§9.9 与 §10 的 `AIConfig` 键说明同步。**纠正掉的 6 条过期结论**：`real` 永不可达（D-1 后本地图片已读盘内联 base64，`real` 是真实存在的状态）、`export-csv` 对全体登录用户开放、`ai-schedule/chat` 一次模型都不调、`chat-relay` 为本地假应答且不扣次数、`/tech/ai-configs` 与 `POST /welfare/gateways` 明文回读密钥。**复核后确认仍然成立、故予以保留的 2 条**：`/minister/*` 与 `viewer_export` 在导出上的 403 不对称、`ai-schedule/apply` 传空班次列表仍会 panic。**过程中还纠了我自己一次**：我一度认定 `viewer_export` 被 Casbin 挡在 export-csv 之外，现场打它得到 200/1561 字节，`git diff` 显示那几行 `/deductions*` GET 是本工作树未提交的新增（我前几轮改的），于是按真实语义写文档而不是把错结论誊进去。

**验证方式**：`go build ./...`、`go vet ./...`、`go test ./...` 全绿（controller/model/service/pkg/ai/pkg/secretbox 五包，含新增 `dorm_inspection_detail_test.go` 6 用例 + 5 子用例，覆盖本楼栋 200 与跨楼栋 404 且不泄漏他栋、不存在与越权**报文逐字节相同**、`全楼`/技术维护组不受限、七种非法编号一律 400、名单按 id 升序且噪声名单不混入、简报不含 `reason`、`structured_json` 五种脏数据）。后端在独立目录 + 独立库跑同一套源码（`go run ./cmd/server`，原因见上一轮"本机环境限制"，杀软问题依旧），浏览器以 `evaluate_script` 真实点验：点卡片打开详情弹窗且字段与库值一致；该宿管账号（1号楼）名下 id 11–14 **请求详情全部 404**，且这四个 id 恰好就是她列表里看不到的那批——楼栋过滤真的在详情这条路上独立生效，不是靠列表挡住了才碰不到；存储型 XSS 回环（把 `<img onerror>` 探针经**真实纠正接口**写进识别文本再打开详情）**执行 0 次、注入节点 0 个**，探针按字面文本渲染；内存修复前后 error 事件计数对照 8081 → 3。**本轮新发现的自身测试陷阱**：Gin 的 `c.Param` 里塞含空白的编号会让 `http.NewRequest` 报 `malformed HTTP version`，所以非法编号分支最初**根本没被执行到**；改成 URL 固定、只经 `c.Params` 注入原始编号，才真正覆盖到解析失败的 400 路径。截图工具本机不可用（`NATIVE_BROWSER_VIEWPORT_UNAVAILABLE`），视觉呈现以 DOM/几何/计算样式断言替代，未经像素级确认。仓库内 `backend/xgh_system.db` 全程未被写入。

**本轮发现但未修（不要声称已完整）**：
1. **历史留痕缩略图仍是全分辨率解码**：手机实拍上传实测 **3060×4080**，在 144px 高的网格卡片里每张解码后仍占约 50MB 位图内存。`loading="lazy" decoding="async"` 只减少**同时解码的张数**，不改变单张体积。根治需要后端缩略图接口（下载时按比例生成小图），属新端点范畴，本轮未做。
2. **打表导出闸门的"持打表权副部长"放行分支只由单测证明**：要走真实 HTTP 就得把某测试账号提升为副部长并写 `users` 表，该写入被策略拦截，我没有绕过。四类放行里浏览器/HTTP 侧实际只覆盖了三类。
3. **人工纠正无法把结论清空**：`applyStr` 对空白值静默跳过，因此"把识别文本改成空"会返回"没有检测到任何改动"。这是既有实现的语义（本轮只记录，未改）；真需要"清除结论"应作为显式操作而不是空串。
4. **上一轮记的一条环境前提本轮已不成立**：上轮写"本机 DNS 被污染且无公网可达的可用端点"，本轮证据（远程兜底图真的加载成功）表明该机器可正常出公网。透传终端"用真实密钥端到端真扣一次上游"仍未做——**缺的只剩一把真实密钥，不再是网络**。
5. 本轮与上一轮的全部改动**至今未提交**，且工作树与并发会话共用（`viewer_export` 的 Casbin 行这类"文档说没有、代码里有"的分歧正是由此而来）。提交需按文件归属逐个 `git add`，并手工排除 `backend/xgh_gov.exe` 这类磁盘残留。

### 本轮已落地（2026-09-26 深夜第二轮，宿舍加扣分落实到人 0/1/2 + 登录页脱敏 + 侧栏点亮）

使用方本轮先定三项口径再动手：**只记违规者**（不整寝连坐）、**历史扣分跟人**（换寝不改已发生的归属）、**内务卫生类跟寝**；范围**只做 0、1、2 三步**，第 3 步（寝室主体与内务分摊派）不在本轮。落点：`static/index.html`（登录区脱敏、打表单点名行与已选徽标、`#filter-deduct-cat` 补第 8 项）、`static/app.js`（`initSidebarClickState`、八个寝室在住名单函数、台账"未落实到人"渲染、提交体带 `student_id`）、`internal/controller/deduction_controller.go`（`sameBuilding` 与 `resolveStudent` 分支顺序）、新增 `internal/controller/deduction_resolve_test.go`（7 用例）。

**A0 台账把"未落实到人"摆到明面**：`unlinked_in_page` 后端早已返回、前端一直没用，所以这一步是**纯渲染**——统计行追加"本页 N 条未落实到人（不计入寝室评优）"，姓名后挂 amber 徽标并把后果写进 `title`（换寝/同名不可追溯 + 不进标兵寝室）。之所以先做这一步：这个字段此前是**沉默的**，副部长打表时看不出自己那条到底绑上了人没有，"落实到人"就无从谈起。

**A1 打表绑定学生主键**：前端按楼栋+寝室号防抖 250ms 调 `GET /students/room-members` 列出在住名单，点选即把 `students.id` 带进提交体，姓名与班级由名册回填；手打姓名与已选不一致、或寝室/楼栋一改动，**主键立即作废**（宁可不绑，也不绑错人）。`GetRoomStudents` 本就返回完整 `model.Student`（含 `id`），因此这一步不需要新端点。

**后端真正的洞在分支顺序，不在功能缺失**：`resolveStudent` 原先先跑 `rosterIsEmpty()` 与 `RoomHasRoster()` 两条**宽松兜底**（名册没录 → 仅按姓名存底放行），显式 `student_id` 的判断排在它们之后。后果是**只要该寝室在 `active` 名册里查无他人，前端点选来的正确主键也会被降级成 `student_id=0` 的匿名记录**，而"这人根本不在本寝"的退宿守卫几乎走不到。这不是测试猜出来的——新写的 7 个用例里有 2 个（寝室不符、学生已退宿）**在改动前直接失败**，才把顺序问题钉出来。修法是把 `student_id > 0` 整块提到函数最前，两条宽松兜底只服务姓名路径。同处补 `sameBuilding()`：楼栋两侧**互为子串即算同栋**，任一侧为空则放行，口径与既有查询侧 `building LIKE '%X%'` 一致——打表单写"1号楼"而名册记"西12号楼"这类写法差异不该变成新的硬拒，但**寝室号相同、楼栋毫不相干**（9号楼901 vs 1号楼901）必须拒。

**B 登录页示例凭证清除**：删掉整块"快速身份预填"（四个 `fillDemo(...)` 按钮与"测试账号一键体验 (密码 123456)"字样）和 `fillDemo()` 本体。真正该注意的是**占位符里藏着另一套完整凭证**：`inp-quick-phone` 的 `测试手机: 13800008888` + `inp-quick-name` 的 `测试姓名: 测试宿管` + `inp-quick-bldg` 的 `测试楼栋: 1号楼` 三样合起来**正好是一组可用的宿管三要素免密登录凭证**（C2 那条通道），只删按钮不改占位符等于没删。同时移掉 `detectDevicePhoneSim()` 里读不到手机号时**代填测试手机号**的兜底——公网部署下这是替别人把三要素凑齐。改为如实提示手工填写。

**C 侧栏点击反馈的根因**：左侧横条是 `body.console-mode .newapi-nav-item.active::before`（`style.css:1203`），也就是**"没反馈"＝那一行拿不到 `.active`**。`switchTab` 的高亮是 `document.getElementById('sidebar-nav-' + tabId)` **反查**，而"部员请假审批"这类入口在 `app.js:861` 注入时**根本没有 id**（同批还有 管理部员/849、AI 排表/857、招新审核/873、标兵榜/877、考场/881 等）；更糟的是它与"部员上工与积分台账"**共用 `tabId='minister'`**，所以点它时高亮不是"不亮"而是**亮到另一行**。修法不逐个补 id（会漏，且下次注入新入口又犯），而是在 `#sidebar-nav-container` 上加一个**文档级委托监听**，以"真正被点的那一枚"为准覆盖一次；内联 `onclick` 先于冒泡阶段的文档监听执行，顺序天然正确，无需改任何一行 `onclick` 字符串。作用域限定在导航容器内：底部"返回主封面/退出当前账号"复用同一 `.newapi-nav-item` 样式类但不代表栏目，不该被点亮。

**A2 的前提被实测推翻，建议改道**：原计划第 2 步是新增 `RoomOccupancy(student_id, building, room_number, bed_number, starts_at, ends_at)` 住宿历史，让打表按事件日取在住快照，从而解除换寝后的硬拒。动手前查了名册写入路径，发现**这套代码没有稳定的学生身份**：`BatchImport` 只有两种模式——追加（`tx.CreateInBatches` 直接新建，同一人重复导入会产生两个 id）与覆盖（`DELETE FROM students` 硬删后重建，`student_controller.go:131`），**没有任何按学号/姓名的 upsert**。于是 `RoomOccupancy` 要挂的 `student_id` 本身就是一次覆盖导入即全量失效的键。现场在独立实例上把它跑实了：先造一条 `student_id=6` 的已绑定扣分记录，再做一次覆盖导入（新名册 id 为 7、8，`AUTOINCREMENT` 序列不回卷故旧 id 不复用），结果**该记录仍写着 `student_id=6`、指向已不存在的人**，台账里 `unlinked_in_page` 依旧是 2——**10 条 / 39 分处于悬空绑定状态，且系统把它们当成"已落实到人"**。影响面按 `student_id` 逐处核对：`excellence_controller.go:62/113` 的硬过滤是 `student_id > 0`，悬空记录**照样计入寝室评优**；而 `deduction_controller.go:658` 的学生违纪详情写的是 `student_id = ? OR (student_id = 0 AND 姓名+班级)`，悬空记录**两边都不匹配**，等于从这个人的档案里**凭空消失**。也就是说覆盖导入后的表现是"榜单里还扣着分、档案里却查不到"，比一开始就是 `student_id=0` 更坏。对照 `ClearAllStudents`：它**显式**把 `student_id > 0` 归零并要求 `unlink_linked=true` 二次确认（`student_controller.go:262-281`），而覆盖导入两条都没有。因此建议把 A2 拆成两步、并按新顺序做：**A2′ 先堵毁链**（覆盖导入前统计并按稳定键回填/降级 `student_id`，口径与 `ClearAllStudents` 对齐，改动小且立刻消除"错数字比空数字更可信"这个失效模式）→ **A2″ 再谈住宿历史**（前提是名册导入改成按 `student_no`（现为普通索引、可选、非唯一）或 `姓名+班级` 的 upsert，使 `students.id` 跨学期稳定；那之后换寝只是同一行的字段变化，住宿历史才建得住）。本轮**未动这部分代码**，等使用方就"A2′ 单独做 / 直接一步做到 A2″ / 维持原 A2 计划"定一下。

**A2′ 已落地（同日，使用方选定"只做 A2′"并补了一条关键业务规则）**：使用方明确 **单个学生每学期分数会被清零，所以一般情况不用考虑换寝室带来的麻烦**。这条直接改变了做法——**既然清零，就不该跨名册重绑**：按 姓名+班级 把上学期的扣分接到新学期同名的人身上是错的。于是 A2′ 收敛为一件事：**覆盖导入后把悬空绑定降级为 `student_id = 0`（仅按姓名存底），不做任何重绑**，口径与 `ClearAllStudents` 一致。落点 `student_controller.go`：新增 `unlinkDanglingDeductions(tx)`（`student_id > 0 AND student_id NOT IN (SELECT id FROM students)` → 0），**只在新行插入完成之后**于同一事务内执行，且**仅在覆盖模式**下调用（追加导入不删人，不产生悬空）。顺带堵掉一个让"降级"失效的洞：`BatchImport` 直接把请求体里的 `[]model.Student` 入库，**客户端自带 `id` 就能让新生占用旧生已失效的主键**，那样悬空绑定会被"合法地"挂到另一个人身上；现在入参主键一律作废、由服务端分配。解除条数进响应（`bindings_released`）、进 message、进审计留痕。新增 `student_import_binding_test.go` 5 用例：覆盖导入解除全部悬空并如实报数、**新学期同名同班同寝也不得重绑**、客户端自带 id 不生效且旧绑定归零、追加导入不动有效绑定、`unlinkDanglingDeductions` 只碰悬空那一类（有效与本就 0 的都不改写）。

**本轮新查到的相邻问题（未修，需使用方定）**：前端 `confirmBatchImport(overwrite)` **从不发送 `overwrite_confirm`**，所以界面上那个"覆盖导入"按钮点下去必然 400，报文还叫调用方带上一个浏览器里没法带的字面量。也就是说覆盖导入**目前只有 API 客户端走得通**——A2′ 的降级因此只在脚本导入时生效。要修只需在该函数里按 `ClearAllStudents` 的做法索取确认串并随请求发出，但那等于**把一条现在实际被挡住的破坏性路径接到界面上**，是否要做请明示。

**验证方式**：`node --check static/app.js` 通过；`go build ./...`、`go vet ./...`、`go test ./...` 全绿（含新增 `deduction_resolve_test.go` 7 用例：楼栋不符拒绝、楼栋写法宽松互含放行、寝室不符拒绝、已退宿拒绝、姓名路径仍拒绝同名、无主键时仍按姓名兜底、`sameBuilding` 真值表；以及新增 `student_import_binding_test.go` 5 用例：覆盖导入解除全部悬空并如实报 `bindings_released`、**新学期同名同班同寝也不得重绑**、客户端自带 `id` 不生效且旧绑定归零、追加导入保留有效绑定、`unlinkDanglingDeductions` 只改写悬空那一类）。后端现场跑在独立实例（`go run ./cmd/server`，PORT 8099，库为 `backend/.verify/xgh_verify.db` 副本），三条真实 HTTP 断言：无主键且名册未登记 → 200 且 `student_id=0`、`unlinked_in_page` 计入；显式主键 → 200 且 `student_id=1`、姓名班级以名册覆盖；**寝室号同为 901 但楼栋填成 1号楼 → 400 且报文明确指出登记于 9号楼 901**（第三条第一轮 400 的原因是漏带 `X-Confirm-Password` 而先撞了 step-up，补上头部后才是真的走到新守卫——这类"看起来通过了"的假阳性值得记）。登录页脱敏以浏览器实时 DOM 断言：`123456`/四个演示账号名/`13800008888`/`测试宿管`/`快速身份预填`/`fillDemo` **九个特征串在整页 innerHTML 中命中数为 0**，五个输入框占位符逐一读到新文案。仓库内 `backend/xgh_system.db` 全程未被触碰，临时实例与夹具（`backend/.verify/`）本轮结束时删除。

**本轮发现但未修（不要声称已完整）**：
1. **A1/A0/C/B 的交互层未经目视确认**：`take_screenshot` 本机不可用（`NATIVE_BROWSER_VIEWPORT_UNAVAILABLE`），而两条补足路径都被权限策略拦下——以脚本填演示账号登录被拦（本轮刚把这些凭证删掉，拦截是对的），在页面里给 `request()` 打桩喂假数据以驱动名单渲染也被拦。因此**寝室在住名单的点选渲染、徽标定位、左侧横条的实际视觉**只有源码级与结构级证据，没有真实浏览器里的像素/布局确认。需要使用方在浏览器点一次确认；前端逻辑本身另有 `node --check` 与后端三条真实 HTTP 断言托底。
2. **A2′ 只做完后端一半**：覆盖导入毁链已在服务端堵住（降级 + 入参主键作废，见上节），但**前端 `confirmBatchImport` 从不发送 `overwrite_confirm`**，界面上的覆盖导入按钮点下去必然 400，所以这条降级**目前只在脚本/API 客户端导入时生效**。是否要把确认串接进界面（等于放开一条现在实际被挡住的破坏性路径）见上节，待使用方定。
3. **第 3 步（内务卫生类跟寝）落地仍缺主体**：使用方已定"内务、卫生类跟寝"，但寝室在本系统里**没有实体表**，只有 `(building, room_number)` 字符串；本轮点选名单是**按寝室列出人**，不等于"寝室自己有一条加分/扣分台账"。该口径真正兑现需要第 3 步，本轮范围外。
4. **"历史扣分跟人"本轮无需迁移**：扣分记录本就同时存 `student_id` 与事件时点的楼栋/楼层/寝室快照，所以"跟人"在数据上是既成事实；这也说明 A2 的价值全在**换寝之后仍能绑定**这一点上，而它卡在身份稳定性上：`students.id` 一次覆盖导入即全量失效，要先做 A2″ 的"按稳定键 upsert"（前置量见第 5 条）才谈得上住宿历史。
5. **`StudentNo` 不是唯一索引**（`models.go:333` 为 `index`）且导入侧未必填充，故"按学号 upsert"能不能落地取决于使用方名册里学号的实际覆盖率——真要走 A2″ 需先量一下现有名册 `student_no` 的空值比例。**本轮试图在仓库内量这个数，结果是量不到**：`backend/xgh_system.db` 的 `students` 表 **0 条**，且其 `deduction_records` **还没有 `student_id` 列**（早于 B1 的过期快照）。也就是说仓库里这份库既不能用来验证名册相关行为，也不能作为"A2′ 毁链到底影响多少条"的依据——**真实数字必须到部署侧库上跑只读统计**（本轮已把统计 SQL 化好，见 `.verify/roster_stats.py`，随本轮夹具一并删除，需要时可复现）。
6. 本轮及前几轮改动**仍未提交**，工作树与并发会话共用（`git status` 里 `pkg/ai/client.go` 与 `scripts/stress_dorm.py` 即非本会话产物）。

### E-2 部分实现，与文档描述有偏差

| 文档位置 | 偏差 |
|---|---|
| 3.1 时段任务卡片 | 后端配置化设计是**不错的**，但 `PeriodType` 枚举三处不一致：模型注释 `weekday/weekend/single_week/double_week`、种子数据写 `daily/weekend`、判定逻辑却比较 `"weekdays"/"weekends"`（`dorm_controller.go:234-238`）。→ **周末规则永不生效，"周内工作日"与单双周类型完全未实现**。**（2026-09-26 ✅ 已修，见上轮 ②；同轮发现 `GenerateSchedule` 的 `rule_type` 只认 `weekday`，单双周排班仍不按周交替——属遗留，未修）** |
| 4.2 双模态处理 | "无 AI 原图直发"现在如实标 `disabled`（A 组已修）。~~因 B4 未做，副部长拿不到名单~~ → **B4 已落地**：纸条/纯文本上报的名单会落 `inspection_subjects`，打表可直接从名单选人，无需重新手打。**（2026-09-26 晚补充：宿管端识别后新增人工纠正编辑框，改动逐条落 `review_note` 留痕、仅限本人、已转扣分记录 409 拒改，且不篡改 `ai_status`，见「晚」轮 ④）** **（2026-09-26 深夜补充：该卡片下方"所辖楼栋历史拍照留痕"现支持点击单条查看详情——`GET /dorm/inspections/:id` 一次返回记录本体、记名名单、已关联打表简报与结构化归纳；因宿管在 Casbin 上持有 `/dorm/*` GET|POST 通配，楼栋过滤在本接口内逐字重放列表口径，且"不存在"与"不属于你的楼栋"合并成同一个 404 报文，避免退化成记录探测面，见「深夜」轮 ①）** |
| 4.3 四维搜索 + CSV | 楼层/寝室/姓名/班级筛选与 CSV 导出**确实可用** ✅ |
| 5.2 标兵公示 | 最高积分/最优上工标兵是**请求时实时计算**（`minister_controller.go:487-499`），非文档所述"每周自动评定并公示"，无快照留存。**（2026-09-26 ✅ 已修，见上轮 ④：评定落快照、公示只读快照、按周留痕，部长与部员端同源；余下"每周自动"仅缺定时器触发）** |
| 5.2 部长灵活调分 | 实现了，但**无额度上限、无理由校验、无复核** → 积分通胀会直接侵蚀评优公信力。**（2026-09-25 补充：本轮新增的出勤 ±5 自动结算同样无上限、分值写死在 `service` 常量里，未做校级参数配置——两类加分通道并存后，调分审计更值得先补）**。**（2026-09-26 ✅ 已修，见上轮 ③：校级策略表配额度、理由校验、复核线、清单与冲正）** |
| 6.1 插画素材工坊 | 只读聚合外部图库，无本地素材管理与审核 |
| 6.2 福利兑换 | 网关/定价/额度/透传一整套都在，**但依赖 D-2 SSRF 与 D-3 明文密钥先解决**。**（2026-09-26 ✅ 两个前置均已闭合：SSRF 见 D-2，明文密钥见本轮 ⑤——密文入库、接口只回掩码、启动回写历史明文。兑换/定价/额度/透传逻辑本身此前即为 ✅；仍未验证的部分是"用一把真实上游密钥跑通端到端扣次数"，需要真密钥，本轮测试只用了假密钥串）** **（2026-09-26 晚补充：前端"高性能 AI 模型透传交互终端"原是本地假应答，现真实调用 `POST /welfare/chat-relay`，失败不扣次数、余额字段读对、历史与转义均已闭合，见「晚」轮 ③；`/welfare/exchange*` 兑换侧的扣积分仍非原子，属未修残留）** |
| 七章 名册杂乱文本粘贴识别 | **已实现且可用** ✅（`parse-preview` + `batch-import`） |
| 八章 ZIP 多合一导出 | **已实现且可用** ✅ |

---

## F. 上线前置（非代码，但缺了上不了线）

1. **未成年人数据合规**：全校学生姓名/班级/床位/违纪事实/照片/手机号集中入库。文档"终生深度留痕"这一表述必须修正为明确的**保留期限**（建议毕业后 N 个月清除），并补**学生本人可见 + 申诉通道**。这条过不了校方审批，功能再全也不能部署。
2. **业务流程落地**：谁来建档（宿管花名册、学生名册）、谁任命技术部副部长、换届如何收权、误扣分的纠错 SLA。**自 2026-09-26 轮 ① 起，"任命副部长"与"派发/回收打表权"是同一个动作**（部长管本部、技术维护组跨部门，均需 step-up 口令并留痕），换届收权即把人调回部员。但**当前仓库库里仍没有任何满足"技术 + 副部长"的账号，除 `tech_admin` 外无人能打表**——上线前必须先用任免接口建这个岗。
3. **工程基建**：目录**已初始化 git**（14 次提交，最早 2026-09-24；系统代码自 2026-09-25 的"初始提交"入库，B/C/D 组改动已随其后的提交进仓，可按 commit 逐组 diff），但**2026-09-26 两轮的改动至今一个都没提交**：`git status` 当前为 **20 个已修改 + 24 个未跟踪路径**，其中本清单落地期间新增、至今未入库的有：2026-09-25 轮的 `controller/{duty,excellence}_controller.go` 与 `service/duty_settlement.go`，2026-09-26 轮的 `pkg/secretbox/`、`static/vendor/`、`controller/{honor,score_policy}_controller.go`、`service/score_policy.go`、`model/{period,position,secret_hooks}_test.go`，以及 2026-09-26 晚轮的 `pkg/ai/client_chat_test.go`、`internal/controller/{welfare_quota,deduction_ledger_reader}_test.go`（晚轮的后端能力多在既有文件内：`controller/dorm_controller.go` 的新纠正接口、`welfare_controller.go` 的透传与扣减、`minister_controller.go` 的真实排班调用、`pkg/ai/client.go`、`static/app.js` 与 `static/index.html`）。**必须先提醒**：这个工作树正被**另一个并行会话共用**（该会话在做移动端与工作台视觉改版），上述计数**不全是本轮产物**——`Dockerfile`、`docker-compose.yml`、`.env.example`、`docs/`、`scripts/*` 等不属本轮。提交前请按文件逐项辨归属，不要 `git add -A`，否则会把别人未完成的改动一起钉进历史。此外仍无 CI、无 lint、无迁移工具（依赖 `AutoMigrate` 启动改建表结构）。建议尽快形成"一组改动一个提交"的节奏，否则下次仍要靠人工还原。**密钥文件已被忽略**：`.gitignore` 现含 `*.key`（由并行会话补上，晚轮提交前请再次确认这一行仍在）。**已更正（2026-09-26 深夜第二轮复核）**：`backend/xgh_gov.exe`（50 MB，未跟踪）**现已命中忽略规则**——`.gitignore:30` 有 `backend/*.exe`，`git check-ignore -v` 确认命中，不再需要手工排除。但**仍然绝不 `git add -A`**，理由换成更实在的一条：工作树里同时存在非本会话产物（`backend/pkg/ai/client.go`、`backend/dist/`、`scripts/stress_dorm.py`），一把 `add` 会把别人未完成的改动一起钉进历史。
4. **构建环境**：`go.mod` 依赖 68 条**全部错标 `// indirect`**（未跑过 `go mod tidy`）；依赖源需 `GOPROXY=https://goproxy.cn`。**驱动现状需注意**：`repository/db.go` 与 `go.mod` 已改用纯 Go 的 `github.com/glebarez/sqlite`（代码注释标明"临时：本机无 C 编译器，跑通后还原为 `gorm.io/driver/sqlite`"），故**当前提交的代码不需要 CGO 即可构建运行**；若原作者要换回 CGO 驱动，请先确认本机有 C 编译器，否则启动即 panic。磁盘上那份 54 MB 的 `backend/xgh_server`（2026-09-19 构建，已被 `.gitignore` 的 `backend/xgh_server*` 忽略、**未入 git**）**不含 B/C/D/E 任何修复，部署前必须重新构建**，别按文件时间戳误认它是新产物。
5. **前端 CDN**：`index.html` 引 `cdn.tailwindcss.com` 与 cdnjs 的 Font Awesome。**校园网拦外网时整站会掉成无样式裸 HTML**——这比"没配 AI"更致命，因为 AI 是可选的、CSS 是必须的。文档把"网络受限也能平稳流转"当卖点，但界面并未做到。需本地化这两个静态资源。**（2026-09-26 ✅ 已落地，见 E 组本轮 ⑥：Tailwind Play 运行时、Font Awesome 6.4.0 全套图标字体、`style.css` 里那条会阻塞解析的 Google Fonts `@import` 三处已全部改为同源 `/static/vendor/`，浏览器实测整页仅剩一张 Unsplash 壁纸为跨域；"整站掉成裸 HTML"的风险已消除。剩余：Tailwind 仍是运行时 JIT（未引入构建链）、主页壁纸与素材工坊图片仍是无本地兜底的外链。）**
6. **交付说明**：`README.md` 启动路径写的是原作者的 `/mnt/资料/...`，与实际目录不符；其中若干功能描述（APK、素养测评、AI 双引擎）与现状有出入，建议同步修订以免误导使用方。

---

## G. 建议实施顺序

**第一批（制度可信）** B1 → B5 → B2 → B3 → B4
理由：外键和撤销留痕是其余一切的地基；B5 防冒名必须等 B1。

**第二批（能安全上线）** C1 → C2 → C3 → C6 → A 组遗留的软删 → C7 审计 → D-3 配置外置

**第三批（补齐宣称功能）** E-1 的素养测评前端、出勤自动加分、旷工判定、寝室评优、招新审核 —— **✅ 2026-09-25 已落地并实测**（见 E-1✅）；E-1 5.1 任免联动权限、E-2 3.1 时段枚举、E-2 5.2 灵活调分收口、E-2 5.2 标兵公示快照、E-2 6.2 配置密钥密文入库 —— **✅ 2026-09-26 已落地并实测**（见该轮）；AI 引擎双卡片互相覆盖与导出中心"提示需登录"两个 BUG、E-1 5.3 AI 对话排班的**真实调用**、E-2 6.2 透传终端的**真实调用**、宿管端 AI 识别结果的人工纠正通道 —— **✅ 2026-09-26 晚已落地并实测**（见「晚」轮）。该批后续顺位建议是 **题库/出题 API**、**`GenerateSchedule` 的 `rule_type` 遗留**（单双周与周末排班仍不生效），以及 **E-1 6.1 播音库界面**（有路由无界面）。**A2′ 覆盖导入毁链已于 2026-09-26 深夜第二轮做完后端**（悬空绑定降级、入参主键作废、`bindings_released` 留痕，见该节），剩余尾巴只有一条：前端 `confirmBatchImport` 从不发 `overwrite_confirm`，界面上的覆盖导入按钮点下去必然 400，因此该降级目前只在脚本导入时生效——接不接这条破坏性路径到界面需使用方明示。若仍要兑现"换寝后扣分仍跟人"的住宿历史（A2″），**前置条件是先把名册导入改成按稳定键 upsert**，否则 `RoomOccupancy` 挂在一个每次覆盖导入即全量失效的键上。

**并行可做** F-5 前端 CDN 本地化 —— **✅ 2026-09-26 已落地**（见 E 组本轮 ⑥，残留见该处第 5 条）；剩余 F-1 合规文本、F-3 工程基建（这两项与业务解耦，风险最低，建议先做）

> 工作量提示：B 组与 C 组都涉及**表结构变更与既有数据回填**，而这个库没有迁移工具、B/C/D/E 的改动至今**仍未形成提交**——真正花时间的是安全地改数据，不是写代码。动手前务必备份 `xgh_system.db`。

---

## 附：本清单依据

**代码规模**（审计基线 → 本清单更新时）：

| 指标 | 审计基线 | 当前 |
|---|---|---|
| `backend/` Go 源码（不含测试） | 约 5.7k 行 | 约 11.4k 行（11,437） |
| 模型结构体 | 22 | 26（`AutoMigrate` 注册同一批 26 项） |
| 控制器文件 | 13 | 22 |
| `cmd/server/main.go` 路由注册 | 约 60 | 103 |
| `static/` 三文件（app.js / index.html / style.css） | 约 7.2k 行 | 约 10.9k 行（10,869） |
| Go 测试文件 | —（基线未清点） | 12（`model/{period,position,secret_hooks}_test.go`、`service/roster_test.go`、`controller/{welfare_quota,deduction_ledger_reader,dorm_inspection_detail,deduction_resolve,student_import_binding}_test.go`、`pkg/ai/{client_p0,client_chat}_test.go`、`pkg/secretbox/secretbox_test.go`） |
| `static/vendor/` 本地化静态资源 | 0（全走 CDN） | 13 个文件 / 1.45 MB（F-5） |

增量来自 B/C/D/E 四组落地改动，以及另一会话并行完成的前端视觉重做与 `mobile-apk/xgh-app`（Android Kotlin 客户端，未纳入本清单审计）。

**验证记录**（均在独立实例 `xgh-live` 中执行，仓库内 `backend/xgh_system.db` 未被触碰）：

- A 组（P0）：13 条 HTTP 断言 + `pkg/ai` 单元测试
- B 组（数据完整性）：52 条
- C 组（登录与会话）：34 条
- D 组（安全）：23 条
- E 组（功能落地，2026-09-25 轮）：71 条
- **E 组（功能落地，2026-09-26 轮）：172 条**（3.1 时段 17 + 5.2 调分与复核 21 + 5.1 任免与权限联动 34 + 5.2 标兵公示快照 34 + 6.2 密钥密文入库与脱敏回传 38 + ⑦ 通用编辑器新建用户与提权回退 28；⑤ 与 ⑦ 两组均重复跑两轮全绿）+ 4 个包的 `go vet`/`go test`
- **E 组（AI 与账务，2026-09-26 晚轮）：57 条** HTTP 断言（导出 Cookie 化 4 类、AI 配置单卡片保存不伤兄弟行、透传终端 11 项矩阵、排班 AI 的 502/503 均不出草案、宿管纠正 happy path + 5 种拒绝 + 归属/404/409/审计）+ 新增 `pkg/ai/client_chat_test.go`（11 个用例/子用例）、`internal/controller/welfare_quota_test.go`（4 个用例，含"并发 8 个请求抢 10 次余额只成功 1 次"）与 `internal/controller/deduction_ledger_reader_test.go`（6 个用例：台账导出放行的四类、拒绝的四类、停用/账号缺失、任命即放行与免职即回收、处理器级 403/200、查库失败不写出半个 CSV 字节）+ `go build ./...`/`go vet ./...`/`go test ./...` 五包全绿
- **F 组 F-5（前端资源本地化）**：不新增 HTTP 断言，验证走浏览器侧资源清单——整页 13 个资源仅 1 个跨域（Unsplash 壁纸），三份样式表均为同源且规则数非零，本地 woff2 强制加载后度量与兜底字体不同。
- **落实到人 0/1/2（2026-09-26 深夜第二轮）**：3 条真实 HTTP 断言（名册未登记→`student_id=0` 且计入 `unlinked_in_page`；显式主键→绑定成功且姓名班级以名册覆盖；寝室号同为 901 而楼栋填 1号楼→400 并指名登记于 9号楼 901）+ 新增 `internal/controller/deduction_resolve_test.go` 7 用例（其中"寝室不符""已退宿拒绝"两条**在改动前直接失败**，由此暴露 `resolveStudent` 的分支顺序缺陷）+ A2 前提的现场复现：一条 `student_id=6` 的已绑定记录经覆盖导入后仍指向已不存在的人，独立实例中 **10 条 / 39 分** 处于悬空绑定而 `unlinked_in_page` 不变。登录页脱敏以浏览器实时 DOM 断言，九个特征串（`123456`、四个演示账号名、`13800008888`、`测试宿管`、`快速身份预填`、`fillDemo`）整页 innerHTML **命中 0**。**本轮交互层未经目视确认**：截图工具不可用，且"以脚本填演示账号登录"与"在页面里给 `request()` 打桩喂假数据"两条补足路径均被权限策略拦下（前者拦得对，凭证正是本轮删掉的），故名单点选渲染与左侧横条仅有源码级与结构级证据。
- **前端浏览器实测：已做**。E 组新增的每处交互（履职核销加分 / 旷工扫描 / 招新审核 / 素养测评交卷 / 标兵寝室榜单与 CSV）均在浏览器中真实点击并断言 DOM 结果，积分变化、`duty_count`、`missed_count`、审核意见落库、判分分值已逐项核对。2026-09-26 晚轮同样以真实点击 + DOM 读数复核四项：保存左卡后右卡未提交输入逐字保留且 DOM 节点未重建、四个导出真实落盘 1705/15863/1965/2138 字节且无"请登录"提示、纠正卡片保存后回"已纠正 3 项"且 `severity` 以 `critical` 落库（该项第一轮暴露了我自己的 `.value` 取值 BUG）、透传终端显示红色失败气泡并带真实拨号错误、`<img onerror>` 注入探针后 DOM 中 IMG 节点数为 0。**边界说明**：应用内 Browser 面板处于隐藏状态，`take_screenshot` 返回视口不可用，因此**视觉像素层面的还原度未经目视确认**，实测结论仅覆盖结构与行为。

**仍未覆盖**：`media/image1.png`、`image2.png` 两张统计图未纳入分析。
