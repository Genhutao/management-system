# 学管会综合管理系统 · 功能严格测试用例集

> 依据：`API接口文档.md`（2026-09 版）+ 2026-09-26 对外合规审计结论。
> 适用版本：`backend/cmd/server` 当前 main 分支。
> 用例原则：**严格断言**——每个用例给出必须逐字/逐值核对的状态码、错误文案或字段值；"大概成功"不算通过。
>
> 优先级：P0 = 主干与安全边界，每轮发布必跑；P1 = 常规回归；P2 = 健壮性/容错。

---

## 0. 测试环境与通用约定

| 项 | 要求 |
|---|---|
| 部署形态 | 与生产一致（容器化 + `JWT_SECRET`/`CRYPTO_SECRET` 显式注入；`GIN_MODE=release`） |
| 测试库 | 独立 SQLite 文件（`DB_PATH` 指向临时文件），每轮清库重置种子数据 |
| 账号 | 每角色至少 2 个：`tech_admin`、`minister`（技术部+非技术部各1）、`member`、`dorm_manager`（楼栋A、楼栋B各1）、`viewer_export`、`disabled` 账号 1 个 |
| 钟点控制 | 涉及时段判定（today-tasks / slot-notice / sweep-missed）的用例需可 mock 服务器时间或临时改配置 |
| 断言基准 | HTTP 状态码 **且** 响应 JSON 键值；写操作还需回查数据库（`operation_logs`、`users.total_score` 等）落库结果 |

通用安全前置用例（每次会话开始先跑）：

| 编号 | 用例 | 预期 |
|---|---|---|
| TC-G-001 | 未带凭据请求任一受保护接口（如 `GET /api/v1/deductions`） | `401`，body `{"error":"请先登录系统"}` |
| TC-G-002 | 带 7 天前签发的过期 token 请求 | `401`，且响应头 Set-Cookie 清除 `xgh_session` |
| TC-G-003 | 带伪造签名 token（改 payload 不改签名） | `401`，文案 `登录已失效或无效令牌…` |
| TC-G-004 | OPTIONS 预检请求（任意 Origin） | `204`；`Access-Control-Allow-Origin` **不出现**，除非该 Origin 在 `ALLOWED_ORIGINS` |
| TC-G-005 | `ALLOWED_ORIGINS` 外的 Origin 发起带凭据的 GET | 响应无 `Access-Control-Allow-*` 头 |
| TC-G-006 | 路径参数含 `../`、`%2e%2e%2f`、Unicode 归一化变体（如 `/api/v1/tech/db/tables/../users`） | `404/400`，绝不 200；不得穿透表白名单 |
| TC-G-007 | JSON body 超大（>8MB）POST 到公开接口 | `400` 或 413，进程不 crash、无 panic 堆栈 |
| TC-G-008 | 全部接口响应头无 `X-Powered-By`/框架版本泄露；`Server` 头不暴露 Gin 版本 | 通过 |

---

## 1. 认证与会话

### 1.1 账号密码登录 `POST /auth/login`

| 编号 | 优先级 | 前置 | 步骤 | 预期（严格断言） |
|---|---|---|---|---|
| TC-AUTH-001 | P0 | 正常账号 | 正确账密登录 | `200`；`token` 非空；响应含 HttpOnly `xgh_session` Cookie（`HttpOnly`、`SameSite=Lax`、`Path=/`） |
| TC-AUTH-002 | P0 | — | 错误密码 ×1 | `401`，文案**逐字**为 `账号或密码错误`（不得区分账号不存在/密码错误） |
| TC-AUTH-003 | P0 | — | 不存在的用户名登录 | `401`，文案与 TC-AUTH-002 **完全一致** |
| TC-AUTH-004 | P0 | disabled 账号 | 正确密码登录 | `401`，文案含 `停用`；**不签发 token** |
| TC-AUTH-005 | P0 | 正常账号 | **连续 5 次**错误密码 | 第 5 次起 `429`，含 `retry_after`（整数秒，>0）；随后用**正确**密码仍 `429`（锁定期内正确密码不得放行） |
| TC-AUTH-006 | P1 | 同上 | 等待锁定时间过后正确登录 | `200`；再错误 1 次 → 重新计数（成功清零计数） |
| TC-AUTH-007 | P1 | — | 同一账号、不同 IP 各错 5 次 | 各自独立锁定（按 `账号\|IP` 计数）；不同账号同 IP 互不影响 |
| TC-AUTH-008 | P2 | — | `username` 前后带空格 `"  admin "` | 按 trim 后账号匹配，`200` |
| TC-AUTH-009 | P2 | — | body 缺 `password` / 非 JSON / Content-Type 错误 | `400` `参数错误，请提供账号和密码` |
| TC-AUTH-010 | P0 | 登录成功 | 服务端登出 `POST /auth/logout` 后用**原 token** 访问受保护接口 | `401`（服务端会话失效，不是仅清 Cookie） |

### 1.2 宿管三要素登录 `POST /auth/dorm-quick-login`

| 编号 | 优先级 | 前置 | 步骤 | 预期 |
|---|---|---|---|---|
| TC-QK-001 | P0 | 花名册有 (手机号P, 姓名N, 楼栋A号楼) | 三要素全对登录 | `200`；`token` 角色=`dorm_manager`；`building` 与花名册一致；审计表出现 `auth.dorm_quick_login` |
| TC-QK-002 | P0 | 同上 | 楼栋传 `"1"`（A号楼的子串） | `403`，**不得**登录（规范化后精确比对）；文案 `楼栋信息不匹配` |
| TC-QK-003 | P1 | 同上 | 楼栋传 `"A栋"`（等价写法，视 normalizeBuildingToken 实现） | 与实现文档一致：要么 200 要么 403，且行为在两处入口（Web/APK）一致 |
| TC-QK-004 | P0 | 同上 | 姓名对+手机号对+**楼栋错** | `403`；**响应体不得包含** `preset_building` 或任何预置楼栋信息（防探测） |
| TC-QK-005 | P0 | 同上 | 姓名+楼栋对、手机号错 5 次 | 第 5 次 `429` |
| TC-QK-006 | P0 | 非宿管角色账号恰好同名同号在花名册 | 三要素命中 | `403`（命中非宿管账号一律拒绝） |
| TC-QK-007 | P1 | 同一手机号重复激活 | 登录两次 | 第二次仍 `200`；users 表**不产生**第二个 `dorm_<phone>` 账号；`user` 对象字段非零值 |
| TC-QK-008 | P0 | 同上 | 自动建号后查 users 表 | 初始密码**不得**为 `123456`（bcrypt 后不可登录 "123456"）——若当前实现仍为固定密码，此用例记为**已知缺陷未修复**，标 FAIL 并挂缺陷号 |
| TC-QK-009 | P2 | — | `floor` 传任意值 | 被忽略，不影响结果；不写库 |

### 1.3 安全设置 `PUT /auth/security-settings`

| 编号 | 优先级 | 前置 | 步骤 | 预期 |
|---|---|---|---|---|
| TC-SEC-001 | P0 | 登录 | 改密码不带 `old_password` | `400` |
| TC-SEC-002 | P0 | 登录 | `old_password` 错误 + 新密码合法 | `401` |
| TC-SEC-003 | P0 | 登录 | 正确旧密码 + 新密码 5 位 | `400`（<6 位） |
| TC-SEC-004 | P0 | 登录 | 正确改密后用**旧 token** 访问 | 按文档旧 token 满 7 天有效 ⇒ 断言当前行为并**记录在案**；若已实现吊销则断言 401（两态择一，禁止"有时 401 有时 200"） |
| TC-SEC-005 | P1 | 两个账号 | 把 `username` 改为他人用户名 | `409` 用户名被占用 |
| TC-SEC-006 | P1 | 宿管账号 | 改手机号不带 old_password | `400`；带正确 old_password 后查 `dorm_roster_presets` 绑定手机号**已同步** |
| TC-SEC-007 | P2 | 登录 | 请求体塞 `role:"tech_admin"`、`total_score:9999` | `200` 但查库 `role`/`total_score` **不变** |
| TC-SEC-008 | P1 | 登录 | 改密后立即重新登录 | 新密码 200、旧密码 401 |

---

## 2. 宿管上报（纪检主干）

### 2.1 上报 `POST /dorm/upload-photo`

| 编号 | 优先级 | 前置 | 步骤 | 预期 |
|---|---|---|---|---|
| TC-UP-001 | P0 | 宿管A(1号楼) | 合规 multipart：image(1MB png) + room_number=101 + photo_type=violation + subject_names=`张三\n李四、王五` | `200`；`record.image_url` 以 `/uploads/` 开头且文件名含 uuid 前缀（非客户端原名整段可预测）；`subject_total=3`；`ai_status` ∈ {real, disabled, failed} 且与 AI 配置实际状态一致 |
| TC-UP-002 | P0 | AI 未配置 | 同上（无 AI 配置） | `ai_status="disabled"`；`record.category/deduct_points/severity` 全为零值；`status="uploaded"` |
| TC-UP-003 | P0 | AI 已配置可用 | 合规上报 | `ai_status="real"`；`structured_result` 五键齐全；`deduct_points` ∈ [0,30]（超出视为模型越界，记录缺陷） |
| TC-UP-004 | P0 | — | 不带 image 且 `report_kind` 为空/`photo` | `400` 现场原图文案 |
| TC-UP-005 | P1 | — | `report_kind="text"` + note_text，无图 | `200`，`report_kind="text"` |
| TC-UP-006 | P0 | — | `room_number=""` | `400` 请填写寝室号 |
| TC-UP-007 | P0 | — | 9MB 图片 | `400` 8MB 上限文案；uploads 目录**无残留文件** |
| TC-UP-008 | P0 | — | `Content-Type: application/pdf` 伪装、`image.exe`、双扩展名 `a.jpg.exe`、超长文件名(>255B) | `400`；uploads 无落盘 |
| TC-UP-009 | P1 | — | note_text 3000 字符 | `200`，库中截断为 2000 字符 |
| TC-UP-010 | P0 | — | subject_names 60 个名字 | `subject_total=50`（MaxSubjectsPerReport=50），多余被截断且响应可感知 |
| TC-UP-011 | P1 | — | `photo_type="任意串"` | 按文档未做枚举校验 ⇒ 断言现状并挂缺陷；**预期修复后**为 400（记 FAIL） |
| TC-UP-012 | P0 | 宿管A | 不传 `building` | 落库 `building` = JWT 中的楼栋（不得为空或他人楼栋） |
| TC-UP-013 | P1 | member 账号 token | 调本接口 | `403`（Casbin 拒绝 dorm 组路由） |

### 2.2 上报详情与人工纠正

| 编号 | 优先级 | 前置 | 步骤 | 预期 |
|---|---|---|---|---|
| TC-DET-001 | P0 | 上报记录R(1号楼) | 宿管A `GET /dorm/inspections/:id` | `200`；`linked_deductions` 含已撤销条目且仅 6 字段；`structured` 为对象或 null |
| TC-DET-002 | P0 | 同上 | 宿管B(2号楼) 访问同一 id | `404`，文案与"记录不存在"**逐字一致**（防跨楼栋探测） |
| TC-DET-003 | P1 | 同上 | `id=abc`、`id=0` | `400` 记录编号无效 |
| TC-DET-004 | P0 | R 为宿管A本人 | A 调 correct，仅传 `severity="severe"` + reason | `200`；`ai_status` 不变；`review_note` 追加了带时间戳的差异说明；`operation_logs` 出现 `dorm.inspection.correct` |
| TC-DET-005 | P0 | 同上 | correct 所有字段省略（无改动） | `400` 未检测到任何改动 |
| TC-DET-006 | P0 | 同上 | `deduct_points=31` / `=-1` / 省略 | 31 与 -1 → `400`；省略 → 合法（不改该项） |
| TC-DET-007 | P0 | 同上 | `reason` 空 / 501 字 | `400` |
| TC-DET-008 | P0 | R 已转打表 | correct | `409` |
| TC-DET-009 | P0 | R 属宿管A | 宿管B 调 correct | `403`；`tech_admin` 调 → `200` |
| TC-DET-010 | P2 | — | `structured_json` 半截 JSON / `[]` / 空串 | 详情 `structured=null`，接口仍 200 |

---

## 3. 打表（扣分）与撤销

| 编号 | 优先级 | 前置 | 步骤 | 预期 |
|---|---|---|---|---|
| TC-DED-001 | P0 | 名册有 张三/1号楼/101 | `POST /deductions` 全字段合法（无 student_id） | `200`；`roster_linked` 与名册命中一致；`operation_logs` 有 `deduction.create`；同事务置上报 `status='converted'` |
| TC-DED-002 | P0 | 名册非空、101 室有登记 | `student_name` 命中名册但 `room_number=102` | `400`，文案含"疑似寝室填报有误或冒名" |
| TC-DED-003 | P0 | 名册非空 | 姓名+寝室都命中 | `roster_linked=true`，记录 `student_id` 已回填 |
| TC-DED-004 | P0 | 名册为空（清库） | 任意姓名寝室打表 | `200` 宽松存底，`message`/审计含标注 |
| TC-DED-005 | P0 | — | `deduct_points=0`、`=-5`、`=""` | 均 `400`（required 对 int 非零） |
| TC-DED-006 | P0 | — | `deduct_points=100000` | 断言现状（当前无上限 → 200），挂缺陷：**无分值上限**；修复后预期 400 |
| TC-DED-007 | P0 | 打表权账号 | 缺 `X-Confirm-Password` 头调 POST /deductions | `403`/`401`（按 §2.3+口令确认实现）；密码错误同拒 |
| TC-DED-008 | P0 | 非打表岗（member） | 调 `POST /deductions` | `403`，不得落库 |
| TC-DED-009 | P0 | 上报R名单含 张三(未转) | 两次 `from-report` 勾选同一 subject | 第一次 `200 count=1`；第二次 `409` 含 `existing_deduction`；库中仅 1 条有效记录 |
| TC-DED-010 | P0 | 上报R属1号楼 | from-report 勾选 2 号楼上报名单的 subject_id | `400` 名单条目与该上报不匹配 |
| TC-DED-011 | P0 | from-report 任一 subject 未过名册核对 | 整批提交 | `400` 带 `subject` 定位字段；**零条**落库（全成全败） |
| TC-DED-012 | P0 | 有效打表D | `POST /deductions/:id/revoke` 带 reason | `200`；记录 `status='revoked'`，原分值与 created_at 不变；`converted_deduction_id` 回退；上报退回可打表态；审计 `deduction.revoke` |
| TC-DED-013 | P0 | D 已撤销 | 再次 revoke | `409` 含 `revoked_by`/`revoked_at` |
| TC-DED-014 | P0 | — | revoke reason 全空白 | `400` |
| TC-DED-015 | P1 | — | `GET /deductions?status=revoked` | 仅撤销记录；`status=foo` ⇒ 静默忽略返回全量（断言现状） |
| TC-DED-016 | P0 | 有效+撤销记录混合 | `GET /deductions` 统计 | `total_deduct_sum` **不含** revoked；`items` 含 revoked；两口径并存不得互相污染 |
| TC-DED-017 | P0 | member 账号 | `GET /deductions/export-csv` | `403`（§2.4 读者闸门）；minister/viewer_export → 200 且 CSV 首行 BOM+表头 16 列逐字一致 |
| TC-DED-018 | P1 | ≥300 条上报 | morning-dorm-reports | `total` 为全量、`items`≤300，分页/口径与文档一致 |
| TC-DED-019 | P0 | ai_status≠real 的上报 | morning-dorm-reports | 该行 `category/severity/deduct_points` 强制清零；`submitted_text` 为人工核对提示 |
| TC-DED-020 | P1 | 同名同班多条名册 | `GET /deductions/profile?name=张三` | `400` 要求补条件；补 class 后唯一命中 |
| TC-DED-021 | P0 | member 无任何条件 | `GET /deductions/student-profiles`（无参数） | `403` 最小必要文案 |
| TC-DED-022 | P0 | 撤销记录存在 | student-profiles 统计 | `revoked_excluded=true`，`total_deduct` 不计撤销 |

---

## 4. 名册（学生）管理

| 编号 | 优先级 | 前置 | 步骤 | 预期 |
|---|---|---|---|---|
| TC-ROS-001 | P0 | minister | parse-preview 粘贴 3 行规范文本 | `200`；`all_parsed` 字段与原文一致；**缺省编造值行为记录在案**（缺楼栋→1号楼等），每行 `source_line` 可回溯 |
| TC-ROS-002 | P0 | minister | batch-import `overwrite=true` 无 confirm | `400` 含 `previous_count` |
| TC-ROS-003 | P0 | minister | `overwrite_confirm="REPLACE_ALL_ROSTER"` | `200`；事务后 students 表仅新数据；审计 `student_roster.overwrite_import` |
| TC-ROS-004 | P0 | 已有打表记录关联学生 | clear 无 unlink_linked | `409` 含 `roster_count`/`linked_deductions` |
| TC-ROS-005 | P0 | 同上 | clear + `unlink_linked:true` | `200`；扣分记录保留且 `student_id=0`；审计 `student_roster.clear_all` |
| TC-ROS-006 | P0 | member | `GET /students` | `403`（仅 tech_admin） |
| TC-ROS-007 | P1 | 150 名学生 | `GET /students` | `total=150`（不截断）而 `items≤100`；`grade_stats` 与筛选条件无关（断言现状） |
| TC-ROS-008 | P0 | member(非宿管) | `GET /students/room-members?room_number=101` | `200`；`phone` 形如 `138****5678`；宿管本人 → 明文（按文档口径） |
| TC-ROS-009 | P1 | — | batch-import 传 `id` + 覆盖导入 | 断言现状（客户端可指定 id）；确认不产生主键冲突 500 |
| TC-ROS-010 | P2 | — | 覆盖导入后查 `inspection_subjects.student_id` | 断言现状（悬空引用为已知缺陷，挂缺陷号） |

---

## 5. 排班 · 请假 · 积分

| 编号 | 优先级 | 前置 | 步骤 | 预期 |
|---|---|---|---|---|
| TC-SCH-001 | P0 | minister | 生成周一~周五排班 plan | `generated_count` 与 `GET /schedules` 当期条数一致；跳过周末（rule_type=weekday） |
| TC-SCH-002 | P1 | minister | `rule_type="foo"` | 断言现状：不报错、按每天排（缺陷：枚举不校验）；修复后 400 |
| TC-SCH-003 | P0 | member | 对**自己的** shift 提请假 | `200`；leave.status=`pending`；leave-list 可见 |
| TC-SCH-004 | P0 | member | 对他人 shift_id 提请假 | 断言现状（当前不校验归属 → 200，挂缺陷）；**修复后** 403 |
| TC-SCH-005 | P0 | minister | 审批通过 + auto_substitute | 替班人 `total_score +3`；`duty_substitute` 流水 1 条；`schedule_shifts.member_names` 已替换 |
| TC-SCH-006 | P0 | 同上 | **重复审批同一条 leave** | 断言现状：重复加分（缺陷：非幂等）；修复后第二次 409 且分值不变 |
| TC-SCH-007 | P0 | 班次成员含 张三丰 | 请假人姓名 `张三` | 替换**不得**误伤 张三丰（子串替换边界）；审批后 张三丰 仍在 member_names |
| TC-SCH-008 | P0 | minister | `scores/adjust` 无口令头 | 403/401（X-Confirm-Password）；带对 → `total_score` 与 log.balance_after 一致 |
| TC-SCH-009 | P1 | — | `score_change=0` | `400`（int required 非零）；`=-100000` → 断言现状可负分（缺陷挂案） |
| TC-SCH-010 | P1 | member 改名 | my-shifts | 断言现状：按姓名 LIKE ⇒ 改名后看不到原班次（缺陷挂案） |
| TC-SCH-011 | P2 | — | `GET /minister/week-duty-status` | `date` 字段 ≥6 字符时不 panic；红色旷工仅在 `status='missed'` 时出现 |
| TC-SCH-012 | P0 | AI 未配置 | ai-schedule/chat | `503 {"ai_status":"disabled"}`，**无编造内容** |
| TC-SCH-013 | P0 | AI 可用 | chat 正常输入 → apply `shifts:[]` | chat `ai_status="real"`；apply **不得 500/panic**：当前为已知 panic 缺陷 ⇒ 记 FAIL 挂缺陷号 |
| TC-SCH-014 | P0 | member | `GET /member/score-history` | 仅本人数据；`total_score` 与 users 表一致 |

---

## 6. 导出

| 编号 | 优先级 | 前置 | 步骤 | 预期 |
|---|---|---|---|---|
| TC-EXP-001 | P0 | viewer_export | download-csv | `200`；`Content-Disposition` 文件名含时间戳；同一秒两次下载不互相覆盖文件（或断言现状+挂缺陷） |
| TC-EXP-002 | P0 | member | 任一导出接口 | `403` |
| TC-EXP-003 | P1 | 含手机号的记录 | daily-duty-csv 内容 | 断言输出含明文手机号（**合规缺陷挂案**：导出无脱敏、无审计留痕；修复后应脱敏+写 operation_logs） |
| TC-EXP-004 | P1 | 空排班窗口 | bundle-zip"下周排班" | 断言现状回落最早 30 条历史（缺陷挂案：导出不得编造数据） |

---

## 7. 招新 · 公开测评（对外面，重点）

| 编号 | 优先级 | 前置 | 步骤 | 预期 |
|---|---|---|---|---|
| TC-PUB-001 | P0 | — | `GET /recruit/info` 免登录 | `200`；不暴露任何报名者 PII 字段 |
| TC-PUB-002 | P0 | 名册有 张三(高一(1)班,手机号P,101室) | `POST /recruit/apply` 只填姓名+班级+部门 | `200`；库中自动补齐 P/101/性别；**响应体不含** P、101、gender |
| TC-PUB-003 | P0 | 同上 | 传不存在组合 `name=李四&class=不存在班` | `200` 但库中 phone=`班级直报(待联络)`；**不得**因查名册报 500 |
| TC-PUB-004 | P0 | — | 缺 `real_name`/`major_and_class`/`target_department` 任一 | `400` |
| TC-PUB-005 | P1 | — | `target_department="不存在部门"` | 断言现状入库（缺陷挂案）；修复后 400 |
| TC-PUB-006 | P0 | — | 同一姓名+班级连续提交 20 次 | 断言现状均 200（缺陷：无频控，可灌水）；**挂合规缺陷**（公开写接口无限流） |
| TC-PUB-007 | P0 | — | `GET /public/exam/papers` 免登录 | 仅 `is_published=true` 的试卷 |
| TC-PUB-008 | P0 | 存在未发布试卷 id=X | `GET /public/exam/papers/X` 免登录 | 当前**返回 200 泄题**（缺陷挂案，必修）；修复后 404 |
| TC-PUB-009 | P0 | 已发布试卷 | `GET .../papers/:id` | 题目对象**无** `correct_answer` 键（非仅置空） |
| TC-PUB-010 | P0 | 同上 | 免登录 submit 全对 | `200`；`detail_results[].correct_answer` 当前回传（**缺陷挂案**：答案泄露给提交者；修复后断言无此键） |
| TC-PUB-011 | P0 | — | submit 不限次重复提交同名同卷 10 次 | 断言现状全 200 入库（缺陷：无频控、身份自报不可信）；成绩汇总表出现 10 条 |
| TC-PUB-012 | P1 | 10 题以上试卷 | `answers` 用 `"1".."12"` 十进制字符串键作答 | 每题独立匹配，`"1"` 不得命中其它题（rune 转换缺陷回归测试） |
| TC-PUB-013 | P2 | 多选题 | 答案 `"B,A"` vs 标准答案 `"A,B"` | 断言现状判错并挂缺陷（集合比较） |
| TC-PUB-014 | P0 | — | 招新/答题页前端 | **必须**展示个人信息告知与同意勾选（合规 P0）；未勾选提交被前端与后端双重拒绝（后端 400）——当前未实现 ⇒ 挂缺陷 |

---

## 8. 福利 · 积分兑换（资金面）

| 编号 | 优先级 | 前置 | 步骤 | 预期 |
|---|---|---|---|---|
| TC-WEL-001 | P0 | member 配置网关 | `GET /welfare/gateways` | 响应**无** `api_key` 键；`key_mask` 形如 `abcd****wxyz` |
| TC-WEL-002 | P0 | — | `POST /welfare/gateways` base_url=`http://127.0.0.1:8080` / `http://192.168.1.1` / `http://169.254.169.254` | 均 `400`（SSRF 白名单拒绝） |
| TC-WEL-003 | P0 | — | base_url=`http://内网域名`（DNS 解析到私网 IP） | `400`（解析后校验生效） |
| TC-WEL-004 | P0 | member 余额 10 | chat-relay 1 次成功 | 扣 1；DB 原子校验；`ai_status="real"` |
| TC-WEL-005 | P0 | 余额 0 | chat-relay | `503`/余额不足文案；上游即使可用也**不透传** |
| TC-WEL-006 | P0 | 余额 1，**并发** 20 路 chat-relay | 并发请求 | 成功恰好 1 次，余额恰好 0（原子 UPDATE 条件扣减回归）——exchange 系接口当前无事务，**本用例对 `/:id/exchange` 与 `/exchange-model` 必然 FAIL ⇒ 挂缺陷**（双花） |
| TC-WEL-007 | P0 | 上游 500 | chat-relay | `502`；**不扣额度**、响应无编造应答 |
| TC-WEL-008 | P0 | member | `POST /welfare/pricings` | 断言现状 200（缺陷：无角色/部门校验，任何部员可定价）；修复后 403 |
| TC-WEL-009 | P1 | 部门A定价 | 部门B member 调 exchange-model | 断言现状可买他部包（缺陷挂案） |
| TC-WEL-010 | P1 | — | probe-models 上游 401 | 响应**不含** `resp.Body` 原文；不落库编造清单（断言现状挂缺陷） |

---

## 9. 技术运维 · 后台数据编辑器

| 编号 | 优先级 | 前置 | 步骤 | 预期 |
|---|---|---|---|---|
| TC-TEC-001 | P0 | tech_admin | `GET /tech/ai-configs` | `api_key` 键不存在；`has_key`/`key_mask` 正确 |
| TC-TEC-002 | P0 | tech_admin | PUT ai-configs `api_key:""` | 保留原密钥：用新会话调 playground 仍成功 |
| TC-TEC-003 | P0 | tech_admin | `POST /tech/db/tables/users` 携带 `role:"tech_admin"` | `403`；落库角色=member |
| TC-TEC-004 | P0 | tech_admin | `PUT /tech/db/tables/users/:id` 载荷含 `role`、`password_hash` | 两键被剥离；查库不变；错误不再被吞（写失败 → 非 200） |
| TC-TEC-005 | P0 | tech_admin | POST `inspection_photos` 伪造 `ai_status:"real"` | 断言现状可写（**P0 合规缺陷挂案**：绕过"禁止编造 AI 结论"的唯一残留通道）；修复后 400/剥离 |
| TC-TEC-006 | P0 | — | `GET /tech/db/tables/operation_logs`（不在白名单） | `400` |
| TC-TEC-007 | P0 | tech_admin | `DELETE /tech/db/tables/users/1` | `403/400`（id==1 保护） |
| TC-TEC-008 | P1 | tech_admin | task-slots PUT 省略 `is_enabled` | 断言现状规则被静默停用（缺陷挂案）；修复后保留原值 |
| TC-TEC-009 | P1 | — | `start_time="ab:cd"` | 断言现状静默变 00:00（缺陷挂案） |
| TC-TEC-010 | P0 | tech_admin | `GET /tech/operation-logs` | `200`；包含此前所有打表/撤销/改角色的审计行 |
| TC-TEC-011 | P0 | minister | 上述全部 /tech 接口 | `403` |

---

## 10. 宣传与公示（评优评先相关）

| 编号 | 优先级 | 前置 | 步骤 | 预期 |
|---|---|---|---|---|
| TC-PUBC-001 | P0 | member | `POST /publicity/broadcast/news`（自称播音组专属） | 断言现状 200（缺陷：无部门校验）；修复后 403 |
| TC-PUBC-002 | P1 | member | 创建时传 `is_broadcast:true` | 断言现状直接进播报单（缺陷挂案：客户端可决定发布态） |
| TC-PUBC-003 | P0 | 积分已被兑换扣减的部员 | member-push 红榜 | 断言现状仍按 SUM 流水表彰（缺陷：与 total_score 不一致）；修复后口径统一 |
| TC-PUBC-004 | P0 | push-config 开关关闭姓名/分值 | member-push 输出 | 断言现状开关无效仍输出（缺陷挂案：评优公示信息最小化失败） |
| TC-PUBC-005 | P1 | — | push-config 改目标行 | 断言现状永远改第一条（缺陷挂案） |

---

## 11. 静态资源与文件面（对外合规重点）

| 编号 | 优先级 | 前置 | 步骤 | 预期 |
|---|---|---|---|---|
| TC-FILE-001 | P0 | 存在上传照片 URL（来自上报记录） | **免登录** `GET /uploads/<file>` | 当前 200 无鉴权（**P0 合规缺陷挂案**：巡查照片含学生影像/记名纸条）；修复后：未登录 401/403，宿管登录且楼栋匹配才 200 |
| TC-FILE-002 | P1 | 同上 | 带过期/伪造签名访问受保护文件路由 | 401 |
| TC-FILE-003 | P0 | — | `GET /uploads/../xgh_system.db`、`/uploads/%2e%2e%2fxgh_system.db`、`/uploads/..%5cxgh_system.db` | 均 `400/404`，**绝不**返回数据库文件内容 |
| TC-FILE-004 | P1 | — | `GET /` 与 `/static/index.html` | 200；响应无源码路径/绝对路径泄露；index 无内联密钥（`grep sk-` 空） |
| TC-FILE-005 | P2 | — | robots.txt / security.txt | 记录现状；建议补 robots 屏蔽 /uploads |

---

## 12. 审计留痕（operation_logs 覆盖矩阵）

每完成一个写操作用例后回查审计表。**必须存在**的动作：

`auth.login` / `auth.login_failed` / `auth.logout` / `auth.dorm_quick_login` / `auth.security_update` / `user.role_change` / `member.position_change` / `member.score_adjust` / `deduction.create` / `deduction.create_batch` / `deduction.revoke` / `dorm.inspection.correct` / `student_roster.import` / `student_roster.overwrite_import` / `student_roster.clear_all`

**当前缺失、验收时逐项确认（预期新增）**：

| TC 编号 | 动作 | 预期审计 |
|---|---|---|
| TC-AUD-001 | 数据编辑器全部写操作 | 新增留痕（operator/ip/detail 完整） |
| TC-AUD-002 | 任一导出（CSV/ZIP） | 新增导出留痕：谁、何时、导了什么范围 |
| TC-AUD-003 | 请假审批 | 新增留痕 |
| TC-AUD-004 | AI 配置修改 | 新增留痕 |
| TC-AUD-005 | 福利兑换与透传 | 新增留痕（含金额/次数） |

---

## 13. 并发与幂等专项

| 编号 | 优先级 | 场景 | 预期 |
|---|---|---|---|
| TC-CON-001 | P0 | 同一 subject 并发 from-report ×5 | 恰好 1 条有效扣分，其余 409 |
| TC-CON-002 | P0 | 同一 leave 并发审批 ×5 | 分值只加一次 |
| TC-CON-003 | P0 | 同一 revoke 并发 ×5 | 只成功一次 |
| TC-CON-004 | P0 | 登录限流并发：同一账号 50 路错密码 | 锁定后全部 429，无计数竞态放行 |
| TC-CON-005 | P1 | 导出大表（>5000 行扣分）期间持续写入 | 导出完成不超时；CSV 行数=快照行数 |

---

## 14. 缺陷追踪挂案表（写用例时已确认的现状缺陷，修复后翻转断言）

| 缺陷号 | 摘要 | 关联用例 | 严重级 |
|---|---|---|---|
| BUG-A | `/public/exam` 提交响应回传标准答案 + 详情不校验 is_published | TC-PUB-008/010 | 高（公平性） |
| BUG-B | `/uploads` 无鉴权 | TC-FILE-001 | 高（PIPL） |
| BUG-C | dorm-quick-login 自动建号初始密码 `123456`；403 回显预置楼栋 | TC-QK-004/008 | 高 |
| BUG-D | 福利 exchange 双花（无事务）；quota 口径不一 | TC-WEL-006 | 高 |
| BUG-E | 请假审批非幂等重复加分；姓名 LIKE 误命中（张三/张三丰） | TC-SCH-006/007 | 中 |
| BUG-F | 公开写接口（招新/答题）无频控 | TC-PUB-006/011 | 中（合规） |
| BUG-G | 导出无审计留痕、含明文手机号、部分导出编造数据 | TC-AUD-002, TC-EXP-003/004 | 高（合规） |
| BUG-H | 数据编辑器可伪造 `ai_status:"real"` | TC-TEC-005 | 高 |
| BUG-I | ai-schedule/apply `shifts:[]` 越界 panic | TC-SCH-013 | 中 |
| BUG-J | 前端无个人信息告知/同意（PIPL P0） | TC-PUB-014 | 高（合规） |
| BUG-K | 枚举不校验（photo_type/action/rule_type/target_department） | TC-UP-011 等 | 低 |
| BUG-L | publicity 部门校验缺失、推送开关失效、口径不一 | TC-PUBC-001/003/004 | 中 |

---

## 执行说明

1. **一轮完整回归** ≈ 130 条用例：P0 全跑（约 60 条），P1/P2 按变更范围选跑。
2. 挂案用例（第 14 节）按"现状断言"执行并在报告中标注 `KNOWN-FAIL`，不得静默跳过；修复合并后把断言翻转为修复后预期。
3. 涉及外部 AI 的用例（TC-UP-003、TC-SCH-012/013、TC-WEL-004~007）用 mock 上游（本地 HTTP 假端点 + 白名单放行测试域）保证可重复。
4. 测试产生的含 PII 数据留在测试库，跑完即删——测试环境同样适用"含 PII 禁推"约定。
